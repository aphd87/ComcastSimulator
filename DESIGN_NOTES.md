# The Slate — Design Notes

**Renamed from CableOS 2026-07-24** — the simulator spans both TV/Streaming and Movies now, so "Cable" undersold it; "Video Network Portfolio Simulator" is the actual scope. Code, docs, and UI all use VideoOS going forward; historical entries below that predate the rename keep the old name where it's part of the historical record (commit messages, past session summaries) rather than rewriting history.

**Renamed VideoOS → The Slate, tagline dropped "2012," 2026-08-04** — per the user, "The Slate" is the actual industry term for a studio/network's portfolio of shows and films (what students manage each turn), and reads as a case-study title rather than a software product name. The "2012" in the old tagline ("Video Network Portfolio Simulator · 2012") was stale since the 2026-07-27 timeline reshape spread TV networks across 2012-2026 and Movies was never tied to a calendar year at all — new tagline is "Media Portfolio Simulation." Code, docs, and UI all use The Slate going forward; historical entries below that predate this rename keep VideoOS/CableOS where it's part of the historical record.

A Streamlit business simulation for teaching video network portfolio economics through the lens of Comcast/NBCUniversal's networks. Each network's level runs its own real calendar era, each starting exactly `YEARS_PER_LEVEL` after the last (clean handoff, no overlap) — **defaults to 4 years/level** (Oxygen 2012-2015, Bravo 2016-2019, Peacock 2020-2023), **instructor-tailorable per deployment via Streamlit Secrets** (2026-08-03, see README.md's "Instructor settings" section and `utils/game_state.py::_read_years_per_level`/`LEVEL_START_YEAR`). Was a hardcoded 5 years/level (2026-07-27) before today. FERPA-safe: students register with a pseudonym team name only, no PII is stored (see `app.py`, `utils/game_state.py`).

This doc captures the original design intent (from Zach's mechanics brief) reconciled against what's actually implemented, so the two don't drift apart as the codebase evolves.

## ⚠️ TOP PRIORITY — not yet done, read this before anything else below

**Day 2 Deal Mechanics backlog: ALL 12 ITEMS SHIPPED, 2026-08-05.** Phases 1-6 are all tested and committed — see items 1-12 below for the full spec of each. Nothing left in this backlog. The one standing gap across the *entire* multi-week effort is unchanged: **no real human/browser click-through has ever happened, on any feature, in any session** — see the note below, now more true than ever given how much UI this backlog added (AI Production Tools toggle, Research/Social Listening panel, Debut Season selector, the Emmy Buzz section, and now the split Theory grid immediately below). That should be the very next thing done with this repo.

**Theory content split TV/Movies, same session (2026-08-05)** — real bug found and fixed while discussing the landing page: `THEORY_CONTENT` (`utils/game_state.py`) had exactly 5 cards, all TV-only, and the shared `_render_theory_grid()` rendered the identical 5 on the sign-in screen, the TV Theory tab, AND the Movies Theory tab. That meant the Movies Theory tab was showing actively *wrong* framing, not just TV-flavored — "Content Amortization & Cash Flow Timing" describes 12/36-month TV amortization, the literal opposite of Movies' front-loaded, no-amortization cost structure (see `utils/movie_models.py`'s own module docstring). Fixed by adding a `category` field ("tv"/"movies") to every `THEORY_CONTENT` entry and 5 new Movies-specific cards (Risk-Adjusted NPV & Capital at Risk, Windowed Revenue Recognition & Cannibalization, Deal-Participation Waterfall, Game Theory & Competitive Dynamics — ties to the real Talent Partnerships rival-poaching mechanic — and Portfolio Diversification via Concept Type, a deliberate twin of the TV side's HHI card, drawing on the same underlying theory). `_render_theory_grid(category=...)` now takes `"tv"`/`"movies"`/`"all"` — the TV Theory tab passes `"tv"`, the Movies Theory tab passes `"movies"`, and the sign-in screen's "Strategic Foundation" section (unauthenticated visitors see this before registering) passes the default `"all"`, now grouped under "📺 TV / Streaming" / "🎬 Movies" subtitles instead of one undifferentiated 5-card grid. 10 new tests in `tests/test_theory_content.py` (314 passing repo-wide, was 304) — structural checks on the category split plus AppTest coverage confirming each tab renders only its own category's titles (the actual regression this guards against). **Pushed to `origin/main` same session (commit `be74c3f`)** — `main` in sync with `origin/main` as of 2026-08-05, Streamlit Cloud auto-redeployed. This, together with commits `4957de0`/`bcadaa7`/`20c522f`/`c9cca06`/`73b5ad0` (the Day 2 Deal Mechanics backlog above), is everything built this session — all pushed, nothing local-only.

**Live KeyError caught and hardened same session (commit `7d74add`)** — a real user hit `KeyError: 'category'` on the sign-in landing page right after the theory-split deploy went out (most likely Streamlit Cloud caught mid-redeploy from two pushes a minute apart; `THEORY_CONTENT` was verified correct at the exact commit that was live). `_render_theory_grid()` now uses `t.get("category")` instead of `t["category"]` when filtering — a genuinely defensive hardening for the one screen every visitor hits first, not a fix to wrong data. Worth remembering: a landing-page crash blocks registration entirely, so this screen specifically deserves defensive reads even where the rest of the codebase's convention is "trust internal invariants, don't over-validate."

**Leaderboard entry point relocated, same session** — per user feedback that the sign-in page's "🏆 Just check the Leaderboard" button was an oddly-placed, informally-worded escape hatch: retitled to "🏆 View Leaderboard" (with a "No registration required — just browsing" caption) and, more substantively, the "Choose Your Simulation" landing screen (`ss.active_section in (None, "app")`, reached via the "🏠 App" nav button, only ever seen by *registered* users) gained a third peer card — same visual treatment as the TV/Streaming and Movies cards, secondary-styled button (not `type="primary"` like the two simulation choices, since checking standings isn't this screen's main action). The sign-in page's version stays — it's the only leaderboard access an anonymous, not-yet-registered visitor has, a genuinely different audience than the landing screen's already-registered one. 13 new tests (`tests/test_landing_screen.py` + updated assertions in `tests/test_registration.py`, 317 passing repo-wide).

**Theory grid placement — decided, 2026-08-05: keep both.** Discussed whether the full 10-card grid belongs on the sign-in page at all (a visitor hasn't chosen TV/Streaming vs. Movies yet, so half the cards are irrelevant to whichever they end up picking, and it sits above the actual registration CTA) vs. consolidating to only the per-simulation Theory tabs. Explicit user decision: keep it in both places — the sign-in page's full 10-card grouped grid (pitch/preview for a visitor who hasn't committed yet, including instructors evaluating the tool pre-registration) AND each simulation's own filtered 5-card Theory tab (just-in-time reference once actually playing). This is the current, already-shipped state (no code change needed) — settled, not open anymore.

**Next session: start with a real human/browser click-through of the Movies tab specifically** (Greenlight → Release Strategy → Simulate → Results, ideally through Cycle 3 so windowing/the new Last Cycle recap/Production Trouble/Ancillary Markets Surprise all get seen live), per explicit user request 2026-08-03 — put at the top of tomorrow's list. TV/Streaming's own click-through is equally overdue but Movies goes first. This is now doubly true given how much of Phase 1-3's new UI (Talent Partnerships section, Financing Structure, Exhibitor Posture, Pay-1 Licensing selectors) has never been seen live either.

**A real human/browser click-through has never happened on this repo, on any feature, in any session.** Every "Still open" note in every status section below repeats this same gap because it has never actually been closed — AppTest and `pytest` confirm the code executes without exceptions, but that is structurally not the same as confirming the app renders and works in a real browser (layout, CSS, button placement, whether a click actually does the visible thing a student expects). 2026-08-03 raised the stakes further still — a large session of clarity/reordering/new-mechanic changes across both TV/Streaming and Movies, all verified only via `pytest`/`py_compile`, no browser. **Do not treat any layout or click-path change as trustworthy until this has actually been walked through live.**

## Status as of 2026-08-04

**Sports Rights built for Peacock — a real strategic lever, not a cost line, per explicit user framing:** "originals were often used to create profitability, not just acquisition... a surgically created entertainment slate is critical as it is a loss leader that must make up the profitability gap from sports." New `utils/sports_models.py`:
- **Three leagues/tiers** (a small curated set, same posture as `REGIONS`/`GENRE_DEMOS`): NFL Sunday Night Football (Premium, 5-7yr, concentrated 5-month season, sharp post-season churn), Premier League (Mid, 4-6yr, spread over 9 months, softer churn), Summer Olympics (Prestige, 6-7yr, huge spike but *only in a Games year* — `games_every_n_years=4` — every other year of the hold is pure carrying cost with zero direct revenue).
- **Real sealed-bid-first-price auctions** against six seeded rival tech/media firms (Amazon, Apple, YouTube/Google, Netflix, ESPN/Disney, Warner Bros. Discovery, per explicit user request: "all tech/media firm rivals to bid too") — highest bid wins and pays its *own* bid, not the runner-up's, so overpaying (the real "winner's curse") is a live risk.
- **Auction cadence, per explicit user framing** ("sports rights are largely for 5-7 years"... "enable students to bid on sports rights during each year"): every league gets a real first auction at Peacock Year 1 for every team (`cycle_for_year()` anchors to `LEVEL_START_YEAR["peacock"]`, not a fixed global calendar year — guarantees engagement regardless of instructor-configured level length), then re-auctions only at that league's own term boundary. Rival bids and contract terms are seeded off `(league, cycle_index)` only — deliberately no `team_name` in that seed — so it's the same market for every team in a deployment; only the team's own bid decision is per-team session state (`ss.sports_contracts`, `ss.sports_bid_log`).
- **The loss-leader → retention mechanic**: a held contract's annual rights cost hits Peacock's P&L every year regardless; direct revenue only counts in a year the league actually airs, and even then is scaled by `retained_fraction()` — how much the team spent on Peacock originals *that same year* relative to the season's direct-revenue value, floored at each league's passive `retention_floor` (some subs stick around with zero help) and capped at 1.0. This is the literal code path for the user's framing: the subscriber spike only becomes lasting value if the originals slate is strong enough to retain it.
- **Wired additively into `app_pages/simulation.py`'s existing OCF formula** (not a parallel scoring system) — `_preview_pnl`/`_compute_year` gained optional `sports_rev_m`/`sports_cost_m` params folded straight into total revenue/cost, so Complete-phase totals and the official score already reflect it with no further changes needed.
- **Section placement, revised same day per user feedback after using it** ("is this right before entertainment show selection stuff?"): the bidding decision (`_section_sports_rights_bidding`) renders as its own Peacock-only, dynamically-numbered section *before* Greenlighting — a multi-year rights commitment should shape the entertainment slate built around it, not the other way around. The actual P&L/retention math (`_sports_pnl_recap`) still runs *after* Greenlighting (as a small recap card, not a numbered section) since `retained_fraction()` needs that year's finalized Peacock originals spend — splitting "decide" from "see the consequence" across the page was the fix, not moving the whole feature.
- Shows currently-held contracts, this year's sports P&L breakdown, any packages up for auction with a bid input per package, and auction results (won/lost/unclaimed, against which rival). Redo-This-Year undoes any bid placed during the year being redone, same posture as the existing greenlit-show undo.
- **20 new tests** in `tests/test_sports_models.py` (cycle chaining/determinism, team-independence of the market, auction resolution incl. winner's-curse pricing, the NFL/Premier-League loss-leader property at market price, Olympics Games-year vs. off-year behavior, multi-contract P&L summation) — 193 tests passing total (was 173).
- **Not yet verified in a real browser** — same standing gap as every prior session (see top-priority note above), but worth flagging specifically here: this is a genuinely new, moderately complex UI surface (bid inputs, a submit button, conditional section numbering) that has only been `py_compile`/`pytest`-checked, never clicked through live. Next session touching Peacock should walk the Sports Rights section end-to-end before trusting it in front of students.

**Day 2 Deal Mechanics — a 12-item backlog scoped 2026-08-04, building out coverage of a "Movie Business and Deal Mechanisms" teaching note (windowing/PVOD/theme-park economics were already well covered by the existing revenue-window engine; deal-making/financing/participation were not).** User explicitly asked for the full roadmap up front rather than scoping one item at a time, then to implement it phase by phase. Phases are ordered on real dependencies (financial primitives before the mechanics that reference them; the delicate release-timing rework deliberately last so it doesn't destabilize everything built before it; the TV-side item last since it touches a different part of the codebase entirely). Each phase should get its own tested, `py_compile`-clean commit — do not batch multiple phases into one change.

1. **Financing Structure** (Phase 1 — ✅ shipped 2026-08-04) — per-project choice in Greenlight: Self-Finance (full capital at risk, full upside) vs. Territorial Pre-Sales (lower capital at risk, but gives up most international box office to the distributor who fronted the advance) vs. Tax-Incentive Location (cheaper budget, no cap on upside — real-world "closest thing to free money," not manufactured into a symmetric trade-off just for the sake of one). `MovieProject.financing_structure` (default `"self_finance"`, so every existing call site keeps its exact original behavior — additive, same posture as Concept Type).
2. **Distribution/Participation Waterfall** (Phase 1 — ✅ shipped 2026-08-04) — read-only chart in Results (`utils/movie_models.py::participation_waterfall`), sitting next to the existing Revenue Waterfall: shows where the studio's *net* actually goes (talent gross participation off the top, regardless of profitability; capital recoupment; producer net participation on whatever's left; studio residual, which can go negative even on a nominal "win" — verified manually: a $60M Awards/Prestige movie shows negative residual in both its bear AND base cases, positive only in bull). Not a decision, an explainer — same posture as the Sports Rights P&L recap card. `.get()`-guarded in `_results()` so legacy log entries recorded before this feature existed just skip the chart, no KeyError.
3. **Oscar nominations/wins** (Phase 1 — ✅ shipped 2026-08-04) — extends the *already-built* `AWARDS_ELIGIBLE_GENRES`/`AWARDS_CONTENDER_THRESHOLD`/`critical_score` machinery with a new `AWARDS_WIN_THRESHOLD=88` (reuses the same critical_score draw, no new RNG — a win is always a strict superset of a nomination), surfaced in Results, Complete (`compute_movie_notables`'s new `oscar_nominations`/`oscar_wins`), and the leaderboard badges. Manually verified win rate (~3%) is meaningfully rarer than nomination rate (~33%) for Awards/Prestige over 2000 draws.
   - **Re-verified end-to-end 2026-08-04** (per explicit user request to confirm it actually happens in play, not just in isolated draw-rate math): simulated 300 full 3-cycle Awards/Prestige slates through the exact same code path the Simulate button uses (`draw_actual_multiplier` → `draw_critical_reception` → `awards_contender`/`oscar_win` → `compute_movie_notables`) — **68% of simulated teams land at least one Oscar nomination and 11% land at least one win** over a 3-cycle run. Confirmed every touchpoint is genuinely wired, not just present in code: Simulate handler computes it → Results shows the Win/Nomination banner → Complete's Slate Notables shows the OSCAR RECOGNITION tile → `record_attempt`'s `notables=compute_movie_notables(sorted_log)` call carries it into the stored leaderboard entry → `_format_notables_badges` renders the 🏆/🎬 badges there.
   - **Known, deliberate limitation, not a bug**: Oscar eligibility is gated to `AWARDS_ELIGIBLE_GENRES = {"Awards/Prestige", "Drama"}` (matches real-world Oscar dynamics — action/horror/comedy rarely compete for Best Picture-type awards). A team that never picks either genre across all 3 cycles will never see a nomination or win, full stop — the OSCAR RECOGNITION tile just reads "—" for their whole playthrough. Worth an instructor callout if it comes up ("why didn't my tentpole-only slate get any Oscar buzz") — that's the mechanic working as designed, not missing.
   - 22 new tests across `tests/test_movie_models.py`, `tests/test_movies_page.py`, `tests/test_game_state_scoping.py` (215 passing total, was 193) — financing math, waterfall accounting identity (revenue − talent − recoupment − producer = residual), Oscar threshold ordering, backward-compat legacy-log handling, and a UI smoke test confirming the Financing Structure selectbox and Deal Waterfall chart both actually render.
   - **Not yet verified in a real browser** — same standing gap noted throughout this file.
4. **First-Look/Overall Talent Deals** (Phase 2 — ✅ shipped 2026-08-04) — a standing partnership with one of 4 fictional talent collectives (`TALENT_PARTNERS`, modeled on real archetypes — an action-forward producer-star collective, a prestige awards banner, a family/animation banner, a horror specialist — not naming real individuals, unlike Sports Rights' real corporate rivals or the real Bravo/Oxygen/Peacock show titles, since a talent deal is inherently about a person's persona). Signed once for the rest of the level; the partner's star-power or critical-reception bonus only applies when a project's genre matches their specialty (`app_pages/movies.py::_active_talent_bonus`).
5. **Holding Deals** (Phase 2 — ✅ shipped 2026-08-04) — genuinely distinct from #4: a cheaper one-off option to reserve a partner's window for *next* cycle. Resolves in two stages: rival-claim risk checked instantly on placement (a rival studio may have already locked the window — `draw_rival_claim`), and even a claim-free hold has a real chance of falling through by next cycle (`draw_hold_forfeit`, ~30%) — "no guarantee the movie comes together in time," per the user's framing.
   - **Rival-studio game theory, per explicit user request** ("is there any game theory... where you can see how your choices impact rival studios?" + "students should [see opportunities] that may or may not be taken up by rival studios"): partners nobody has claimed (no Overall Deal, no active hold) face a per-cycle chance (`draw_rival_poach`, 12%/cycle) of being permanently signed away by one of 4 fictional rival studios (`RIVAL_STUDIOS`) — passing isn't a neutral no-op. Manually verified pacing: ~78% chance at least one of the four partners is gone by cycle 3 if untouched, ~56% chance a placed hold both clears the rival-claim check and doesn't forfeit.
   - Talent-deal fees are tracked as `ss.movie_talent_total_spend` and shown prominently (Talent Partnerships section running total, Complete-phase summary tile) but **deliberately not folded into `compute_movie_score`'s composite NPV/score** — `_complete()` reconstructs projects fresh from `project_kwargs` and recomputes `risk_adjusted_npv` independently, so there's no single point to inject the deduction without either touching the calibrated engine or duplicating its internal weighting formula outside `utils/movie_models.py`. Flagged as a known simplification, worth a proper fix (e.g. a `talent_spend_m` parameter threaded through `compute_movie_score`) if full NPV integration matters later.
   - 28 new tests (234 passing total, was 215) — partner data integrity (a real bug caught here: Brightlane's specialty was originally `"Family/Kids"`, a `CONCEPT_TYPE` value, not a `GENRE`, so its bonus could never have activated), draw-function rate/independence/determinism checks, and UI tests. The UI tests needed a real fix mid-session: `draw_rival_claim`/`draw_hold_forfeit`/`draw_rival_poach` are seeded off Python's built-in `hash()`, which is **per-process-randomized** (`PYTHONHASHSEED`) unless pinned — a fixed team name's outcome is reproducible within one process run but not across separate `python` invocations, so tests must never hardcode "team X resolves to Y" from a one-off verification script. Fixed by either seeding session_state directly or monkeypatching the draw_* call site; verified robust by rerunning the suite under `PYTHONHASHSEED=0/1/42/12345`.
6. **Pay-1/2/3 Window Licensing** (Phase 3 — ✅ shipped 2026-08-04) — new `pay1_licensing` field on `MovieProject` ("keep" | "license_out"), chosen in Release Strategy. Licensing swaps owned `subscriber_value()` for a flat, pre-negotiated fee (`pay1_license_fee()` = 55% of BASE-case subscriber value, priced before release like a real licensing deal, not indexed to the actual resolved outcome) that arrives sooner in the cashflow timeline (alongside PVOD, not 2 months later). Defensively ignored for Day-and-Date (`is_licensing_out()` returns False regardless of the field) since that strategy already commits the title to Peacock exclusivity as its core premise — enforced in the model itself, not just hidden in the UI.
7. **Exhibitor Split Negotiation** (Phase 3 — ✅ shipped 2026-08-04) — new `exhibitor_posture` field ("standard" | "aggressive" | "exhibitor_friendly"). "standard" reproduces the original fixed `EXHIBITOR_SPLIT`/screens behavior exactly. **A real calibration bug caught by manual QA before shipping**: the screens multiplier and split-rate swing were originally both ±15%/±11.5%-relative, framed as symmetric opposites — but screens scale the box-office base that PVOD/subscriber-value/longtail all derive from, while the split rate only touches theatrical net directly, so an equal-magnitude screens swing structurally swamped the split swing and made `exhibitor_friendly` a near-dominant strategy (+8-17% NPV over standard, no real trade-off) regardless of project economics. Rebalanced the screens swing down to ±8% (split stays ±11.5%) after a manual sweep across a tentpole and an indie drama, narrowing the spread to a defensible ±5-6% either direction — not perfectly neutral (aggressive still edges ahead slightly in both test cases, a structural artifact of screens affecting more revenue lines than split), but no longer an obvious "always pick this one."
   - **Test-infrastructure fix, discovered incidentally while writing these tests**: `draw_rival_claim`/`draw_hold_forfeit`/`draw_rival_poach` (Phase 2) and pre-existing functions in `utils/sports_models.py` and `utils/models.py` all seed off Python's built-in `hash()`, which is per-process-randomized (`PYTHONHASHSEED`) unless pinned. This surfaced as three separate, unrelated-looking test failures across one session (two in code written today, one pre-existing in `tests/test_sports_models.py`, a fourth found while validating the fix in pre-existing `tests/test_models.py`). Added a repo-root `conftest.py` that pins `PYTHONHASHSEED=0` via a `subprocess.run` re-exec if unset (an initial `os.execv`-based version was tried first and silently swallowed the child process's stdout/stderr in this sandboxed Windows dev environment — confirmed via a minimal repro before switching approaches), respecting any explicit `PYTHONHASHSEED` a developer sets on purpose. Also fixed two now-reproducible-under-seed-0 bugs this exposed: an off-by-one in `test_sports_models.py::test_most_years_nothing_is_up_for_bid`'s threshold (compared a 39-element slice's count against a threshold derived from the full 40-element list) and a too-small sample size in `test_models.py::test_independent_of_rating_variance_seed` (100 draws at a 4% event chance carried a genuine ~1.7% false-failure risk; bumped to 1000 draws). Verified robust across `PYTHONHASHSEED` 0-21 and multiple full-suite runs with it unset.
8. **AI Production Tools** (Phase 4 — ✅ shipped 2026-08-05) — a cost/timeline lever in Greenlight (`ai_production_tools: bool` on `MovieProject`), with a plausible quality-risk tradeoff (not a free efficiency win): cuts the budget component of capital at risk ~15% (`AI_TOOLS_BUDGET_SAVINGS_PCT`, stacks multiplicatively with Financing Structure's own discount, on top of the budget component only) and pulls the whole `windowed_cashflows()` timeline forward 1.5 months (`AI_TOOLS_TIMELINE_SHIFT_MO`, same dollar amounts arrive sooner → higher NPV/IRR via less discounting, not bigger numbers) — offset by a capped critical-reception ceiling (`AI_TOOLS_CRITICAL_CEILING_MULT` = 80% of the genre's normal hi bound, threaded through `draw_critical_reception`'s new `ai_production_tools` kwarg) and its own independent setback risk (`draw_ai_tooling_setback`, own seed offset, ~10% chance, milder haircut than Production Trouble). **Real, deliberate consequence surfaced during manual QA, not a bug**: an AI-tooled Awards/Prestige project's capped ceiling (~83) sits entirely below `AWARDS_WIN_THRESHOLD` (88) — it can still land an Oscar nomination but can never win. Default `False` reproduces every existing call site's exact original behavior.
9. **Research / Social Listening** (Phase 4 — ✅ shipped 2026-08-05) — direct Movies-side parallel to TV/Streaming's `research_revealed` mechanic (`app_pages/renewal.py`, `utils/models.py::preview_show_variance`/`preview_regional_signal`): pay $4M (added directly to the draft's `pa_spend_m` — a real cost, same "changing the default `value=` argument re-keys the un-keyed widget" mechanism the Indie-Horror budget cap already relies on) to preview the actual seeded box-office (`draw_actual_multiplier`) and critical-reception (`draw_critical_reception`) signals for the currently selected Genre/Concept Type, live-updating as either changes, before committing budget. **Simpler than TV's version**: both draws are pure functions of `(team, cycle, genre, concept_type)`, not TV's per-show sequential RNG consumption — no replay-safety/ordering plumbing needed, calling them to preview is exactly as safe as the real Simulate-time call. New `multiplier_to_stars()` in `utils/movie_models.py` maps the previewed multiplier to a genre-aware 1-5 star signal (mirrors TV's `variance_to_stars`, but genre-aware since Movies' bear/bull band isn't one fixed global range). `ss.movie_research_paid` resets on Restart Slate, same posture as TV's `research_revealed` reset on Restart Level/Advance.
10. **eWOM & Piracy** (Phase 4 — ✅ shipped 2026-08-05) — a new independent post-release risk axis (`draw_ewom_piracy_swing`, own RNG seed offset, same pattern as `draw_production_trouble`/`draw_ancillary_surprise`/`draw_ai_tooling_setback`), swinging PVOD *and* owned Peacock subscriber value together (both are "digital" revenue) up or down — deliberately more common than Production Trouble/Ancillary Surprise (15% vs. 5%/12%) since social chatter and piracy leakage are everyday market forces, not rare production-side shocks. `ewom_mult` threaded through `windowed_cashflows`/`npv`/`irr`/`total_revenue`/`participation_waterfall`, default 1.0 = no-op. Deliberately does **not** touch `pay1_license_fee()` when licensing out — a real licensing deal is priced before release, not indexed to how digital word-of-mouth/piracy actually plays out (same reasoning as `PAY1_LICENSE_DISCOUNT`'s own docstring). **Incidental correctness fix caught while wiring this in**: the Results outcome dict's `sub_value` field (feeds the Revenue Waterfall chart) was unconditionally `subscriber_value(multiplier)` even when `pay1_licensing == "license_out"`, silently showing the *not-taken* owned-streaming number instead of the real `pay1_license_fee()` actually earned — fixed to branch on `is_licensing_out()`, same as every other revenue-line read already does.
    - Phase 4 total: 32 new tests (170 passing for `test_movie_models.py`+`test_movies_page.py`, was 138 at Phase 3's close; 278 passing repo-wide). All three items verified with a direct manual smoke test of the actual numbers (capital-at-risk deltas, cashflow timing shifts, critical-reception ceiling clamping, fire rates, revenue-isolation deltas) before committing, not just `pytest` green — same discipline Phase 3's exhibitor-posture bug was caught with.
    - **Not yet verified in a real browser** — same standing gap as every prior session (see top-priority note above).
---
11. **Seasonality / Debut Timing** (Phase 5 — ✅ shipped 2026-08-05) — new `debut_season: str` field on `MovieProject` (`DEBUT_SEASONS` = Off-Peak/Winter/Spring/Summer Tentpole/Fall-Awards/Holiday), chosen in Release Strategy (rendered first, so its crowding/genre-fit effect is visible on every release-strategy preview card below it — `project_base` is rebuilt from the live selectbox value in the same render rather than the stale pre-selector `project`, a real ordering bug caught before committing: naively reading the old `project` would have shown every preview card one interaction stale). Three real effects, same "Off-Peak is the true neutral baseline" posture as every other lever in this file (`SEASON_OPENING_MULT["Off-Peak"]=1.0`, no genre synergy entry, `SEASON_AWARDS_RECALL["Off-Peak"]=1.0`, `SEASON_AWARDS_BUMP_MONTH["Off-Peak"]=11.0` — the original hardcoded value): (1) `SEASON_OPENING_MULT` — Summer Tentpole/Holiday open bigger (more competition too), Winter/Fall-Awards open softer; (2) `SEASON_GENRE_SYNERGY` — an extra opening bonus only when a genre's real audience actually shows up that season (Action/Sci-Fi/Animated in Summer, Animated/Comedy at Holiday, Awards/Prestige/Drama in Fall); (3) `SEASON_AWARDS_RECALL` scales `awards_season_bump()` — real-world recency bias, a Summer release rarely gets recalled by awards voters even with great reviews (0.40x) vs. Fall/Awards or Holiday's full 1.0x, and `SEASON_AWARDS_BUMP_MONTH` moves the actual cashflow timing of that bump to match (Fall/Awards lands at 3mo, Holiday at 2mo, vs. Off-Peak's flat 11mo). Manually verified: Summer opens biggest (18.2 vs. 15.5 baseline for a $40M Awards/Prestige test project) but its awards bump (0.47) is smaller than Fall/Awards's (1.02) despite the bigger box-office base, because the recall penalty dominates — a genuine, teachable trade-off, not just flavor text. 29 new tests (289 passing repo-wide).
    - **Not yet verified in a real browser** — same standing gap as every prior session (see top-priority note above).
12. **TV/Streaming Emmy Tracking** (Phase 6 — ✅ shipped 2026-08-05) — the TV-side parallel to #3, genuinely greenfield: TV had no prestige/awards signal at all before this (only Movies had the critical-reception draw). New `draw_emmy_reception(team, year, show_id, genre)` in `utils/models.py` — own independent seeded draw (own hash offset, never perturbs the rating-variance `rng` sequence `preview_show_variance()` replicates, or `draw_production_risk_event`'s sequence), only rolled for shows that actually aired at full value that year (`status == "active"` in `_compute_year`). Gated to `EMMY_ELIGIBLE_GENRES = {"Drama", "Scripted", "Comedy"}` — the genres with a real-world Emmy prestige-category analogue (Drama/Limited-Anthology/Comedy Series); Reality/Competition/Talk/True Crime return `None` outright (a real eligibility gate, mirrors `AWARDS_ELIGIBLE_GENRES`, not a soft discount) — end-to-end-verified through the real `_compute_year` code path (not just the isolated draw function) that a Reality-genre show never gets a non-`None` score even across 300 simulated teams. `EMMY_NOMINATION_THRESHOLD=70`/`EMMY_WIN_THRESHOLD=87` (a win is a strict superset of a nomination, same posture as Oscar tracking). Per-show `emmy_score`/`emmy_nomination`/`emmy_win` land on every `show_rows` entry; per-year `emmy_nominations`/`emmy_wins` totals land on the `_compute_year` return dict. Surfaced in: a new "🏆 Emmy Buzz" section in Results (top-5 eligible shows by score, badge for WIN/NOMINATED/neither); the pre-Financing `_last_year_recap` strip (a 🏆/🎬 badge, `.get()`-guarded for legacy entries); `compute_level_notables` (new `emmy_nominations`/`emmy_wins` fields, summed across `yearly_log`, defaulting to 0 — not `None` — for a non-empty log missing the keys) and its "EMMY RECOGNITION" tile in the Complete-phase Level Notables (5th tile, same shape as Movies' OSCAR RECOGNITION tile); `_format_notables_badges` in `app_pages/leaderboard.py` (🏆/🎬 badges, same wins-over-noms posture as the Oscar badges next to it). 15 new tests across `utils/models.py`, `app_pages/simulation.py`, `utils/game_state.py`, `app_pages/leaderboard.py` (304 passing repo-wide, was 289 at Phase 5's close).
    - **Not yet verified in a real browser** — same standing gap as every prior session, see the top-priority note above. This closes out the entire 12-item Day 2 Deal Mechanics backlog.

## Status as of 2026-08-03

**A long, mostly-clarity-and-parity session across both games, driven by the user clicking through the locally-running app (`http://localhost:8511`, Anaconda Streamlit) and flagging things in real time.** 159 tests passing (was 144 at session start), all changes verified via `pytest`/`py_compile` only — **no browser click-through happened this session either**, see the priority note above.

**TV/Streaming clarity pass:**
- `app_pages/finance.py`: Ad Revenue metric gained a `help=` tooltip defining a rating point (1% of the 18-49 demo, Nielsen's standard) and the full revenue formula; the Rev/Point vs. Cost/Point benchmark table's caption now defines both terms, not just their interpretation.
- `app_pages/forecast.py`: added a definition + a real caveat to "OCF by Phase" — the three phase windows (Year 1-3/4-7/8-10) are hardcoded in `phase_label()` and don't shift with the Bravo/Peacock Launch Year sliders on the same page, so a custom launch year can land inside a phase window that doesn't yet include that network. Not fixed, just now disclosed.
- `app_pages/simulation.py`: new `_starting_position()` card, shown only at Year 1 (no real prior year exists yet) — computes the incoming roster's baseline P&L (default $5M marketing, no cancellations) and labels it explicitly as a baseline, not an official actual, distinct from `_last_year_recap()` (year > 1, real `yearly_log` data).
- `utils/models.py`: new static `GENRE_DEMOS` lookup (age/gender/reach per genre — True Crime, Reality, Competition, Talk, Scripted, Drama) surfaced as a one-line "Demos:" caption on every Renewal card, right after Proj OCF.

**TV/Streaming reorder, per explicit user request:** `app_pages/renewal.py`'s section order is now: Genre Decay → Research → **IP Value vs. Projected OCF** (moved up, right before the decision matrix) → Renewal Decision Matrix / Your Slate cards → Budget Impact → Full Renewal Analysis Table → **Primetime Scheduling** (moved here from `app_pages/schedule.py`, which is now cash-flow/amortization reference only) → **This Year's Schedule** (moved to sit right after it), which now also shows a **debut-month picker** for any show in `ss.greenlit_ids_this_year`. `tests/test_schedule_page.py` moved its primetime-grid coverage to test `app_pages.renewal.render()` instead of `app_pages.schedule.render()`.

**Registration form:** bigger text boxes (`st.container(key="registration_form")` + scoped CSS in `utils/styles.py`, doesn't touch compact inputs elsewhere), plus a new optional **Class Abbreviation** field (`ss.class_abbrev`, max 20 chars) — threaded through `record_attempt()` as its own field (deliberately NOT part of the `(school, class_section, team_name)` identity/scoping key) and shown in parentheses on every Leaderboard row when present, so it's actually usable for finding a team, not just captured and hidden.

**Movies parity + two new randomness axes, per explicit user request:**
- `app_pages/movies.py`: new `_last_cycle_recap()` card at the top of Decisions, shown for cycle > 1 when a real prior `movie_log` entry exists — the Movies-side parallel to TV's Last Year's Actuals. Cycle 1 has no equivalent (no inherited slate to baseline against, unlike TV's Year 1) so it simply doesn't fire there.
- `utils/movie_models.py`: **Production Trouble** (`draw_production_trouble`) — a movie-flavored parallel to TV's `draw_production_risk_event` (director/actor/VFX/producer setbacks), ~5%/movie/cycle, own independent seed offset. Sized differently from the TV version on purpose: TV can zero one show's revenue and the other ~19 shows absorb it; a movie IS the whole cycle's bet, so this applies a 0.60-0.85x haircut to the resolved box-office multiplier instead of a full zero.
- `utils/movie_models.py`: **Ancillary Markets Surprise** (`draw_ancillary_surprise`) — genuinely movie-specific, no TV-side counterpart at all (TV has no PVOD/rental window or theme-park/merchandise line). ~12%/movie/cycle, can swing PVOD rental and theme-park/merchandise revenue 0.6x-1.6x, independent of both box office and critical reception. Threaded through via new optional `pvod_mult`/`theme_park_mult` params on `windowed_cashflows()`/`npv()`/`irr()`/`total_revenue()` (default 1.0, no-op for every pre-existing caller — verified by `TestAncillaryMultipliersWireIntoRevenue`).
- Both new events surface as distinct callouts in Results (🎬 Production Trouble, 🎢 Ancillary Markets Surprise), and Production Trouble reframes the headline's "landed near your X Case" text since a haircut can push the resolved multiplier below the labeled Bear Case.
- **Important, disclosed to the user rather than silently left implicit**: neither new axis (nor the pre-existing box-office variance draw) feeds `compute_movie_score()` — the official pass/fail score is deliberately computed from bear/base scenario formulas + critical reception, not the literal per-cycle resolved draw. This is pre-existing architecture (critical reception already worked this way), not something changed today. Both new events affect what a team *sees* in Results and the Leaderboard's per-team slate comparison, but not the official score, by design.

**Level length: 5 → 4 years/level, now instructor-tailorable per deployment, per explicit user request.** `utils/game_state.py::YEARS_PER_LEVEL` is no longer a bare constant — `_read_years_per_level()` reads it from this deployment's own Streamlit Secrets (`YEARS_PER_LEVEL = N` in `.streamlit/secrets.toml` / Streamlit Cloud's Settings → Secrets), same BYOK-style per-school mechanism as `ANTHROPIC_API_KEY` in `utils/ai_grading.py` — appropriate since this ships as a business case run by many independent school deployments, not one central instance (see README.md's deployment note). Defaults to **4** when unset (every deployment until an instructor opts in), clamped to `[2, 8]` against a typo'd secret. `LEVEL_START_YEAR` is now computed off `YEARS_PER_LEVEL` (Oxygen fixed at 2012, Bravo/Peacock each start exactly `YEARS_PER_LEVEL` years later) so clean-handoff/no-overlap holds for *any* configured level length, not just 4 or 5. Read once at module import — matches an instructor-set deployment secret, which doesn't change without a redeploy anyway. Documented in README.md's new "Instructor settings" section; 6 new tests in `tests/test_years_per_level_config.py` (default, override, both clamp directions, missing-secrets-file fallback, and the start-year relationship holding for any level length).

**Pushed to GitHub 2026-08-03** across 4 commits (`4fa4b34` TV/Streaming clarity + Renewal reorder, `66d17ef` registration + Class Abbreviation + instructor-configurable level length, `cc91a74` Movies parity + the two new risk axes, `feed7e2` docs) — `main` in sync with `origin/main`, 165 tests passing. Whether the live Streamlit Cloud deployment has actually picked this up is unconfirmed — same standing gap as every prior push, see "Still open" below.

**Still open** — same top-priority gap as every prior session, now larger: no real browser click-through of any of today's changes (registration form, both reordered Renewal sections, the two new Movies risk axes, the two new recap cards, the level-length change), and no confirmation the Streamlit Cloud deployment picked up today's push. See the top-priority note above — Movies goes first next session.

## Status as of 2026-07-30 — read this first

**Left sidebar removed entirely, per explicit user request** ("We don't need a left hand panel, but people should be able to sign in and see leaderboards for TV and movies respectively"). User reported "I can't see the left panel at all, and I can't register my team either" — one root cause, not two: registration and the App/Leaderboard/TV/Movies nav both lived exclusively in `st.sidebar`, so anything that made the sidebar fail to render (a collapsed sidebar with no way back open is a real Streamlit failure mode — `GLOBAL_CSS`'s `header{display:none}` rule hides the same `<header>` element that carries Streamlit's built-in "reopen sidebar" arrow in some versions, though this wasn't conclusively isolated as *the* trigger before the user asked to just drop the sidebar instead) took both features down together. Rather than keep debugging the sidebar, moved everything out of it:
- **Team Registration** (University/School/Class/Team Name + Register button) now renders in the main content area, centered, above the welcome/theory screen — this is the new "sign in" entry point.
- **Top-level nav** (🏠 App / 🏆 Leaderboard / 📺 TV-Streaming / 🎬 Movies) is now a horizontal button row in the main content header, shown once `ss.registered` is true. The TV network sub-selector (Oxygen/Bravo/Peacock, with lock icons) moved the same way, as a second row shown only when TV/Streaming is active.
- `st.sidebar` is no longer called anywhere in `app.py`; the now-dead `[data-testid="stSidebar"]` CSS rules were removed from `utils/styles.py`.
- Leaderboards for TV networks and Movies already existed as separate tabs within the single Leaderboard section (`app_pages/leaderboard.py` — Overall/Oxygen/Bravo/Peacock/Movies tabs) — no change needed there, satisfies the "see leaderboards for TV and movies respectively" ask as-is.
- `tests/test_registration.py` updated: widget queries changed from `at.sidebar.text_input(...)` to `at.text_input(...)` now that the form lives in the main tree. One test (`test_registration_shows_the_simulation_nav_afterward`) hit the documented AppTest + `st.rerun()` stale-widget limitation once moved out of the sidebar block (sidebar apparently dodged it, the main tree doesn't) — fixed by seeding `session_state` directly and using a single `.run()`, the same workaround already established elsewhere in this suite, rather than touching the app's `st.rerun()` usage. 143 tests passing.
- **Not yet verified in a real browser** — same standing gap as every prior session (no Playwright/Claude-in-Chrome click-through has ever happened on this repo). This change is a structural layout change with real UI-placement risk (centered columns, button row wrapping at narrow widths); a real click-through is more important than usual before trusting this in front of students.

**Follow-up same day: duplicate Leaderboard entry point on the post-registration landing screen, spotted by the user** ("why is there on one page 'Leaderboard' and at the bottom just check the leaderboard?"). Confirmed: the top nav's 🏆 Leaderboard button and the landing screen's "Just check the Leaderboard" button did the exact same thing (`ss.active_section = "leaderboard"`) — a leftover from before the nav redesign above made Leaderboard reachable from everywhere. Per the user's follow-up ask, **moved (not duplicated) the button to the pre-registration sign-in screen**, and made the leaderboard genuinely reachable without registering — a visitor can now browse rankings before creating a team:
- `app.py`'s top-level content gate changed from `if not ss.registered:` to `if not ss.registered and ss.active_section != "leaderboard":`, so the leaderboard branch wins when an anonymous visitor clicks through.
- Added a "← Back to Sign In" button on the Leaderboard page when `not ss.registered` (the normal nav row that would get them back doesn't render pre-registration).
- `app_pages/leaderboard.py::render()`: the "My Team Status" cards are skipped entirely when `not ss.registered` (there's no real team to show status for), and the scope radio now defaults to "All Schools" for anonymous visitors instead of "My Class" (which would resolve to an empty school/class and show nothing).
- New `tests/test_registration.py::test_leaderboard_reachable_without_registering` locks in the click path. 144 tests passing.

**Same follow-up, landing-page copy fix**: the "Strategic Foundation — Business Theory" blurb on the pre-registration screen was still Oxygen's specific mission narrative ("You are the General Manager of Oxygen...") — copied over from the Oxygen level brief, but shown to every visitor before they've even chosen TV/Streaming vs. Movies. Per the user's ask ("can we write something more appropriate... given that it sits on top of business theory... some sort of intro that these are frameworks to consider"), rewritten as a general framing of both simulations sharing one underlying toolkit (BCG Matrix, HHI diversification, cord-cutting S-curve, amortization timing, LTV/CAC — the five `THEORY_CONTENT` cards rendered directly below it), rather than narrating one specific level. Oxygen's own mission narrative is untouched in `LEVEL_BRIEFS` where it actually belongs.

**Session paused here 2026-07-30, resuming tomorrow.** Everything above is committed and pushed to `origin/main` (`5a8befc`), working tree clean, 144 tests passing. **Next session should start with the "Still open" item just below — a real human/browser click-through** — this session made three separate structural/copy changes to the landing and sign-in flow (sidebar removal, anonymous leaderboard access, rewritten intro copy) on top of that same still-unverified gap, which is now larger than it's ever been. Also worth a quick sanity check that the Streamlit Cloud deployment actually picked up today's pushes before assuming any of this is live for students.

## Status as of 2026-07-29

**What happened today, in order:**

1. **Fixed a real, recurring bug: the sidebar showed a flat, duplicate nav list** (finance/forecast/greenlight/leaderboard/movies/renewal/schedule/simulation, all as top-level entries above the VideoOS title) **that the user had already reported "several times."** Root cause, finally found: Streamlit auto-generates a sidebar page for every `.py` file sitting in a directory literally named `pages/` next to the entrypoint script — regardless of the fact that `app.py` already imports those same files as plain modules (`from pages.simulation import render as render_simulation`) to build its own intentional nested tab structure. Both navs were rendering at once the entire time; every earlier attempt to "fix the nesting" edited `app.py`'s manual nav and never touched the actual cause. Fixed by `git mv pages app_pages` and updating every `from pages.X import` / `import pages.X` reference across `app.py`, the three cross-module imports inside `pages/simulation.py` (now `app_pages/simulation.py`), and four test files. 129 tests passed unchanged (pure rename, no logic touched). **Local dev server was killed and restarted from the correct directory** — Streamlit's multipage nav is computed once at process start, so a bare file-watcher reload would not have picked up the rename.
2. **Global text-size fix.** User: *"there should be no squinting in this user experience."* Turned out to be systemic, not a couple of small captions — a tally across every page file found 109 instances of `font-size:9-11px` and 48 more at `12px` (216 total occurrences bumped). Wrote a one-off script (not hand-edited) to raise the floor consistently: `9→13px, 10→11→14px, 12→13→15px, 14→16px`, preserving relative hierarchy rather than flattening everything to one size. `.section-title` (the header class used for every "2 · 🔄 Renewal"-style section marker) got its own explicit treatment beyond the blanket bump — `10px → 19px`, bold, brighter color, thicker rule — so headers read as headers, not just slightly-bigger body text, per the follow-up ask to make section headers specifically stand out.
   - **Found and removed a genuinely dead duplicate `styles.py`** while doing this — a second, never-imported copy of the global CSS sitting at repo root with an entirely different (light) theme than the real one (`utils/styles.py`, dark). The first edit pass targeted the wrong file with zero effect, which is exactly the kind of trap that could cost a future session real time the same way the `pages/` rename bug did — deleted outright rather than left as a landmine.
3. **School field reworked**: the old 2-preset dropdown (Northwestern Kellogg / Indiana Kelley / "Other") replaced with two free-text fields, **University** and **School/College**, combined into the existing `school` identity string (`"{college}, {university}"`) at registration — zero changes needed to any of the leaderboard/attempt-scoping functions in `utils/game_state.py`, all of which already treat `school` as an opaque scoping string. `SCHOOL_PRESETS` removed entirely. (Considered and rejected: a curated full AACSB-accredited-schools dropdown — ~900+ entries that go stale as accreditation changes, real UX cost for a one-time registration field, and I'd have had no live source to verify current accreditation status against. User picked the two-field approach.)
4. **Primetime scheduling made a real mechanic — previously illustrative-only, per explicit user request** ("I want students to experience trade-offs with programming a grid, it should impact ratings bumps and such"). Was a static, auto-assigned-by-rating table with a caption explicitly disclaiming it as decorative (see 2026-07-22's entry below). Now:
   - `utils/models.py`: new `PRIMETIME_DAYS`/`PRIMETIME_HOURS` constants (promoted out of a local variable in `pages/schedule.py`, now shared/importable), `DAY_OF_WEEK_MULT` (Tue/Wed/Thu strongest — real primetime convention; Fri/Sat weakest — the industry's actual "death slot" nights; Sun/Mon in between), and `slot_rating_multiplier(day, hour)`, which combines that with the *already-existing, already-calibrated* `HOURLY_INDEX` (no new hour-weighting invented) and rescales the full 28-slot grid's raw spread into a **[0.85, 1.15]** band — sized deliberately against the existing ±7% seeded rating variance in `_compute_year()`, so a scheduling choice matters at least as much as the luck draw, without being able to single-handedly blow through a level's OCF-margin pass threshold.
   - `Show` gained `slot_day`/`slot_hour` (default `None`) and `schedule_multiplier()` — unassigned stays at neutral `1.0`, the same "deliberate zero-effect baseline" precedent as Movies' New IP concept type, so every show's pre-existing calibrated numbers are untouched until a student actually makes a scheduling choice. `ad_revenue()` now folds this in directly.
   - `app_pages/schedule.py`'s "Scheduling Grid" section rebuilt around `st.data_editor` with a `SelectboxColumn` per day — each cell picks one show (by `"Name (#id)"`, unique even across cross-network name collisions) or "— none —"; edits write straight onto the real `Show` objects living in `ss.oxygen_shows`/etc. (same "mutate the object directly" pattern `renewal.py` already uses for `air_month`), so the placement is live for that year's `_compute_year()` call. Double-booking a show across multiple cells is detected and surfaced as a soft warning (last slot placed wins) rather than blocked.
   - **Trade-offs stated up front, per explicit user request** — a callout above the grid states the exact slot count (28), the one-show-per-slot constraint, the real best/worst slots by name, and the exact +15%/−15% stakes, before the student touches anything.
   - 10 new tests (`tests/test_models.py::TestPrimetimeScheduling`, `tests/test_schedule_page.py`) — band-edge exactness, weeknight-beats-weekend ordering, neutral-when-unscheduled backward compatibility (every show built before today keeps its exact original `ad_revenue`), and an AppTest smoke test confirming the new grid renders with no exceptions. 139 tests passing total.
5. Local server restarted twice today (once after the rename, again after the scheduling/font changes) so changes were actually visible rather than silently sitting in edited-but-unloaded source — same lesson as item 1.

**Still open:**
- **No real human/browser click-through has ever happened.** Today's fixes were verified via `py_compile`, the full `pytest` suite, and a local server restart/log check — not a real browser session. Given today's flat-tabs bug was itself something no automated check would have caught (Streamlit's multipage nav is a startup-time behavior, invisible to `pytest`/AppTest, which never spins up the real dev-server process), this gap is now even more clearly the single most important next step, not less. Claude in Chrome still not connected this session (not asked); Playwright still not installed.
- **Pushed to GitHub 2026-07-29** (commit `e1bcad0`, then `30e0bcc`; `main` in sync with `origin/main`) — all four of today's fixes, including the flat-tabs rename, are now on the remote. **Whether the Streamlit Cloud deployment actually picked this up is still unconfirmed** — the live URL still redirects to a `share.streamlit.io` viewer login wall (re-checked 2026-07-29), so even though auto-deploy-on-push is the documented setup, nobody has actually logged in to confirm today's push built and deployed cleanly. Whoever owns the Streamlit Cloud account should check the deploy log.
- **Distribution model resolved 2026-07-29, per the user: this is the per-school-deployment model already described in `README.md`** — this specific live URL is Darden's own instance, gated to Darden students specifically, not a single shared URL meant to verify "any school that purchased the case" (that would need real licensing/auth tooling that doesn't exist and isn't being built). **Still needs a real operational decision, not a code change**: Streamlit Community Cloud's viewer-access list is either individual emails (real per-term maintenance as rosters change) or, if Darden students share a common email domain, a one-time domain-level allow-list — much less overhead, but needs confirming Streamlit Cloud still supports domain-based restriction and that Darden's actual student email domain is known. Whoever manages the Streamlit Cloud account needs to check Settings → Sharing and decide. **Superseded by the next bullet if Darden ends up self-hosting** — this Streamlit-Cloud-viewer-list approach is only relevant if the tool stays on Streamlit Community Cloud at all.
- **Real-time multi-device team play + Darden-hosted access control — scoped 2026-07-29, blocked on infrastructure answers, not yet built.** User confirmed this needs to be **true multi-editor** (all teammates click/edit live from separate devices, changes merge for everyone) rather than the cheaper read-only-spectator alternative that was proposed — a materially bigger build: needs a persistent backend process and a real shared datastore, not Streamlit's current single-process/no-shared-state model, plus a conflict-resolution policy for simultaneous edits. User also confirmed **this will run on Darden's own servers**, not (or not only) Streamlit Community Cloud, which changes the access-control question raised in the bullet above — Darden-hosted infrastructure could use Darden's own SSO instead of Streamlit Cloud's email allow-list. Neither the collaboration engine nor the access-control layer can be responsibly designed without knowing: (1) whether Darden's server is a box we control (persistent process + real database) or more constrained/managed hosting, (2) whether Darden has an existing SSO system (Shibboleth/SAML/campus login) to integrate with vs. a simpler standalone login, and (3) whether future non-Darden schools get their own separate deployments or need centralized license verification. An email draft covering exactly these three questions (plus a timeline ask) was written for the user to send to Darden Business Publishing — **not yet sent as of this entry**. Explicitly on hold per the user ("we'll talk later about this") — do not start building either the collaboration engine or the access-control layer until DBP's answers come back.

**Registration flow investigated 2026-07-29 — code confirmed correct, no bug found, real click-through test added.** User reported being unable to register a team in the sidebar and asked for an immediate fix. Investigation, in order:
- First diagnostic attempt used `AppTest.from_function(script)` with `import app` inside `script()` — this produced a completely blank render tree (zero sidebar widgets, zero main-area elements) after clicking Register, with no exception raised anywhere, which looked exactly like a real, serious crash.
- That result was itself an artifact of the test, not the app: `app.py` is a bare top-level script (no `render()` function, unlike every `app_pages/*.py` module), and `import app` only executes a module's top-level code on the *first* `import` — Python caches it. Every subsequent `AppTest.run()` after that (e.g. the post-click run) silently no-ops instead of re-executing app.py fresh, so the "blank tree, no exception" result reflected the test's own import-caching bug, not a real app failure. Diagnosed by comparing against the exact same test rebuilt with `AppTest.from_file("app.py")`, which re-executes the script's actual text every run (the same mechanism Streamlit's real script runner uses) — with that fix, a full registration click-through (filling University/School-College/Class/Team Name, clicking "Register Team →") succeeds cleanly every time: `registered=True`, correct `team_name`/`school`/`class_section`, zero exceptions, zero validation errors.
- **No code changes were needed** — the registration logic itself was correct the whole time. Most likely real-world explanation for what the user saw: a stale browser session left over from one of today's earlier server restarts (Streamlit's frontend can desync from the server process after a restart without a hard refresh). Server was restarted a third time as a clean baseline; user advised to hard-refresh the browser tab.
- **New `tests/test_registration.py`** (4 tests) locks this in for real going forward: full successful registration, confirms the post-registration nav actually appears, and confirms both a missing-team-name and a missing-university submission are blocked with the right error message rather than silently succeeding or crashing. This is also the first genuinely-external, from-a-blank-slate click-through test of `app.py` itself (every prior AppTest coverage in this repo drives individual `app_pages/*.py` `render()` functions directly, never the full entrypoint script) — worth reusing this `AppTest.from_file("app.py")` pattern for any future `app.py`-level click-path testing rather than the `import app` pattern that produced the false signal here. 143 tests passing.

## History — 2026-07-27 end of session (superseded by 2026-07-29 above)

**What happened today (chronological):**
1. Added a 4th 🏠 App nav button (landing screen was previously unreachable after leaving it, see "App structure" below) and wired the two previously-orphaned `pages/finance.py` (P&L/OCF) and `pages/forecast.py` (10-Yr Forecast) pages in as new tabs under TV/Streaming.
2. Triaged Zach Schlessel's three unresolved structural asks: budget sliders confirmed staying dollar-based (no change needed), AI-graded custom show concepts built (`utils/ai_grading.py`, Claude Haiku 4.5, `client.messages.parse` with a Pydantic `ShowConceptGrade` schema — originality/market-fit/feasibility/presentation, each 0-25 — wired into `pages/greenlight.py` right before the title/IP legal-risk check). **BYOK, per school**: no API key is ever committed to this repo; each school's own Streamlit Cloud deployment sets its own `ANTHROPIC_API_KEY` in that deployment's secrets, and `api_key_configured()` gates the whole feature off (plain "ask your instructor" message, not a crash) when unset. Documented in the new `README.md` as an action item to flag to Darden — this tool is going out to schools worldwide, so costs must be per-school. `anthropic>=0.100.0` added to `requirements.txt`. Movies category rework (sequels/new-IP/kids/horror-indie) stayed unscoped — see "Still open" below.
3. **Reshaped the TV/Streaming timeline to real per-network calendar eras**, per explicit user request: Oxygen 2012-2016 ("saving a sinking channel"), Bravo 2017-2021, Peacock 2022-2026 — a clean handoff at each boundary, no overlap (user's initial ask had Peacock starting 2021 while Bravo still ran through 2021; confirmed as unintentional and moved to 2022-2026). Mechanically this was smaller than it first looked: all three eras span the same 5 calendar years, so it's a single `YEARS_PER_LEVEL: 4 → 5` bump, not three different level lengths. New canonical constants in `utils/game_state.py`: `YEARS_PER_LEVEL = 5`, `LEVEL_START_YEAR = {"oxygen": 2012, "bravo": 2017, "peacock": 2022}` — previously `YEARS_PER_LEVEL` was a local constant duplicated in `pages/simulation.py`, and calendar year was a uniform `2011 + year` formula that made *every* network's Year 1 display as 2012 regardless of which network was active (since `ss.year` resets to 1 on every level transition — confirmed via full codebase research before touching anything). `_year_label()` in `pages/simulation.py` now takes the active network and looks up `LEVEL_START_YEAR[net] + (year - 1)`; `_compute_year()` and `_progress_bar()` both threaded an extra `net` parameter to reach it. `app.py`'s `LEVEL_BRIEFS` mission text for all three networks now derives its "repeat for N years (start–end)" copy from these same constants instead of a hardcoded "4 years" (three separate copies, all stale before today) — Oxygen's mission text also reframed around the "sinking channel" narrative the user asked for. Confirmed independent and unaffected: amortization windows (36mo/12mo/36mo — not numerically coupled to level length), the distribution-fee escalation cap (still never reached within a 5-year level, caps at year 6), Movies/Day 2's cycle-based timeline (no calendar concept at all), and the separate decorative "10-Year Forecast" page (`pages/forecast.py`, its own illustrative `ten_year_sim()` model, not wired to real `ss.year` — deliberately left as-is, out of scope for this ask). 46 tests still pass throughout.

4. **Movies category rework, built.** Resolved the open mechanics question from item 2 above: Concept Type (New IP / Sequel / Family-Kids / Indie-Horror) is a second selector alongside Genre in the Day 2 Greenlight screen, layered onto the existing engine rather than replacing it — see `utils/movie_models.py`'s new "Concept Type" section. New IP is the deliberate zero-effect baseline (so every project built before this feature existed, including all prior tests, keeps its exact original calibrated numbers — this caught a real bug mid-build: an initial version gave New IP its own marketing-lift multiplier, which silently shifted every pre-existing project's economics since New IP is also the dataclass default; fixed by making New IP truly neutral and telling its "relies on marketing" story through the *absence* of Sequel's/Kids' bonuses instead). Sequel gets a per-cycle opening-weekend bonus that decays with franchise fatigue (`SEQUEL_OPENING_BONUS_BY_CYCLE`); Family/Kids gets a softer opening but a stronger long-tail (`KIDS_OPENING_MULT`/`KIDS_LONGTAIL_MULT`, the real "vault" shelf-life effect); Indie-Horror caps the budget slider at `INDIE_HORROR_BUDGET_CAP_M` ($25M) and widens bear/bull variance further on top of Horror-the-genre's own already-wide spread (`scenario_multipliers_for()`, threaded through `draw_actual_multiplier`/`nearest_scenario_label` so the resolved outcome respects the wider bounds too). 6 new tests in `tests/test_movie_models.py::TestConceptType`; one existing Day 2 widget-count assertion updated for the new selector. 52 tests passing total.

5. **UX/prompt-clarity QA pass across TV/Streaming and Movies**, per explicit user request ("make sure students have CLEAR and easy to understand and defined queries/prompts"). Read every decision-flow file end to end (`pages/simulation.py`, `renewal.py`, `greenlight.py`, `schedule.py`, `finance.py`, `forecast.py`, `movies.py`) rather than guessing at problem spots. Real issues found and fixed, not just copy polish:
   - **`pages/schedule.py`'s "Show" selector in the Premiere Day Scenario tool did nothing** — a student could pick any show from the dropdown, but the Episode Count/Cost inputs next to it were always generic fixed defaults (10 eps / $750K), completely disconnected from the selected show's real numbers. Fixed: selecting a show now pre-fills its actual `episodes`/`ep_cost_k` (still editable) — the widget keys are scoped to the show name so switching the dropdown loads fresh defaults instead of carrying over a previous typed override.
   - **`pages/schedule.py`'s Scheduling Optimizer caption told students to "Adjust via Show Editor"** — no such feature exists anywhere in the app (confirmed via repo-wide grep). Replaced with an honest caption: the grid is auto-assigned by each show's fixed `rating` attribute (verified `s.rating` is never reassigned anywhere in the codebase — nothing a student does actually changes this grid), so it's now framed as illustrative-only rather than implying a control that doesn't exist.
   - **`pages/renewal.py`'s per-show Decision dropdown and Premiere-month input both had `label_visibility="collapsed"`** — a first-time student would see a bare selectbox/number box with no visible caption, only a hover tooltip, on the single most consequential control on the page (Renew/Watch/Cancel). Un-collapsed both with short visible labels ("Your decision", "Premiere month (1-12)") plus richer help text explaining what each choice actually does (e.g. the 25% sunk-cost cancellation penalty).
   - **`pages/forecast.py`'s "10-Year Strategy" blurb uses a totally different year-numbering scheme** (continuous Year 1–10, Bravo added "Year 4", Peacock "Year 8") **than the real per-network calendar eras from item 3 above** (each network restarts at its own Year 1, on real 2012–2016/2017–2021/2022–2026 calendars) — a student coming from the real game would find the two numbering schemes genuinely contradictory. Left the model itself untouched (per item 3's explicit "out of scope" call — this is a separate illustrative tool, not a mechanics change) but added a clear disclaimer that this tab is a separate what-if model with its own timeline, not the real game.
   - Swept for other stale references (`grep`'d for `TODO`/`FIXME`/`coming soon`/`Portfolio tab`/`Quick Checklist`/etc. across the repo) — none found beyond the two fixed above.
   - `pages/finance.py`'s own small illustrative "Distribution Revenue Calculator" (10-year, `2011+y` calendar) was checked too but left alone — unlike `forecast.py`'s blurb, it never claims to represent Oxygen/Bravo/Peacock specifically, so there's no real ambiguity for a student to trip on.
   - 52 tests still pass throughout — none of these were mechanics changes, only UI copy/wiring.

6. **Five new TV/Streaming mechanics, closing out the last of Zach Schlessel's brief items that were re-surfaced by re-reading the full verbatim brief** (see the "Zach Schlessel feedback" section below — several of these were never triaged at all in earlier sessions, only just now noticed). All new logic lives in `utils/models.py` with its own seed offset per feature (none share or perturb `preview_show_variance()`'s exact RNG sequence, which the Research feature depends on replicating precisely). `tests/test_models.py` created from scratch (`utils/models.py` had no dedicated test file before today, unlike `utils/movie_models.py`) — 25 tests across all five features below, 71 passing total.
   - **Research now sometimes reveals a regional/demographic signal** alongside the star rating — `preview_regional_signal()`, a small curated `REGIONS` dict (6 regions, illustrative median age/household income, genre-affinity weighted), automated (nothing hand-authored per show), reveals 1-2 regions about half the time. Wired into `pages/renewal.py`'s existing Research expander.
   - **Minimum marketing spend warning** — Zach's brief specified a "$3M marketing minimum for show viability"; Financing now warns (soft, not a block) when marketing-per-active-show falls below `MIN_MARKETING_PER_SHOW_M`.
   - **Amortization reassigned from network to genre** (the single riskiest item on the list, previously flagged twice across two sessions as needing a scope decision before touching it) — True Crime now amortizes over `TRUE_CRIME_AMORT_MONTHS` (24mo), everything else linear over the existing 12mo, replacing the old Oxygen=36mo/Bravo=12mo network-based split. Peacock/SVOD stays a uniform 36mo (a distinct accounting convention, not a genre one — explicitly not folded into the True-Crime split). **Real calibration risk found and verified safe before shipping, not after**: Oxygen's slate is 17/20 True Crime, so its amortization cost rose +71% ($20.2M → $34.5M at Year 1) — but `distribution_revenue()` isn't network-scoped and dominates Oxygen's P&L (a pre-existing characteristic, not something introduced here), so the actual OCF-margin impact is only about −5 points against a 12% threshold sitting around 80% either way. `TestGenreBasedAmortization::test_oxygen_slate_amortization_change_leaves_a_large_margin_cushion` pins this so a future revenue-model change that shrinks that cushion fails loudly instead of silently making Oxygen unpassable. The `amort_months` field was removed from the `Show` dataclass entirely (superseded by the new `effective_amort_months()` method) and all 30 now-obsolete `amort_months=36` kwargs stripped from `utils/data.py`.
   - **Production-risk events** — "shows can fail for legitimate reasons (talent departure, poor script, etc.)" from Zach's brief, previously unimplemented. `draw_production_risk_event()`: a rare (~4%/show/year), independently-seeded event that zeroes a show's revenue for the year and applies the existing 25%-sunk-cost treatment (same as a mid-year cancellation) — but, deliberately, the show stays on the roster for next year's Renewal call rather than being permanently dropped (one bad year, not a career-ending one; also sidesteps a much bigger foundational gap — see "Known gap, not built" below). Surfaced as a distinct 🎲 callout in Results, separate from student-chosen cancellations.
   - **Mid-year emergency budget shift** — one of Zach's brief's own "Open Simulation Decisions" that was never actually answered ("Should budget constraints shift mid-year?"). `draw_emergency_budget_shock()`: ~15%/year chance of a 10-25% cut to `ss.level_budget`, network-level (not per-show), applied once per year via `ss.emergency_shock_years` tracking so re-rendering the Financing step on every Streamlit rerun doesn't re-subtract it. Surfaced as a banner at the top of Financing.

   **Known gap, not built — flagged rather than silently expanded into**: while scoping production-risk events, found that **Greenlighting never actually adds a new show to a team's real roster** — `pages/greenlight.py` is a standalone P&L sandbox/calculator; a concept built there never becomes part of `ss.oxygen_shows`/etc. or affects real score. This means Zach's "shows available in specific years (production delays)" and a true new-show-creation risk mechanic have no real pipeline to attach to yet. Production-risk events above were deliberately scoped to *renewed* shows instead, sidestepping this gap rather than building the (much larger) full new-show-creation pipeline as an unplanned side effect. Worth a real scope conversation before anyone builds on top of it.

7. **Four more items closing out the rest of the "what's next" list** — comparative-analysis leaderboard, AI-grading/Research tie-in, and the two remaining Movies items from Zach's brief. `tests/test_movie_models.py`/`test_game_state_scoping.py` extended (no dedicated test added for the AI-grading extension — that module already had none, consistent with the existing precedent, since it requires a live API call to test meaningfully). 79 tests passing total.
   - **Comparative analysis — view a competitor's slate**, one of Zach's brief's own never-answered "Open Simulation Decisions." `record_attempt()` gained an optional `slate_summary` param, stored as its **own top-level field** on the leaderboard entry (deliberately not merged into `details`, which `pages/leaderboard.py` iterates and number-formats wholesale — a list value there would've broken that loop). Both submit handlers (`pages/simulation.py` and `pages/movies.py`) now pass a lightweight slate — TV: name/genre/network/rating per show; Movies: name/genre/concept type/release strategy/NPV per cycle. `pages/leaderboard.py` renders a "📋 View {team}'s slate" expander per leaderboard row via a shape-agnostic `_format_slate_item()` helper (TV and Movies entries carry different fields). Older pre-existing leaderboard entries gracefully fall back to a "not available" note rather than a blank/broken expander.
   - **AI grading now recommends whether Research is worth paying for** — `ShowConceptGrade` (`utils/ai_grading.py`) gained `research_recommended`/`research_rationale` fields, matching the brief's original ask ("AI grades the concept and determines if testing/research is worth paying for") more closely than the first pass built earlier today. Surfaced in `pages/greenlight.py`'s AI Pitch Feedback panel as an info/caption callout pointing toward the Renewal tab's paid Research option.
   - **Movies: progressive windowing** — the theatrical/platform/day-and-date choice was available from Cycle 1; Zach's brief calls it a "Year 3 Introduction." New `WINDOWING_UNLOCK_CYCLE = 3` constant (`utils/movie_models.py`); `pages/movies.py::_release()` now shows only the Wide Theatrical card (with a "🔒 unlocks at Cycle 3" banner) before that cycle, and defensively forces `release_strategy` back to wide_theatrical so a stale draft value can't sneak a locked strategy through.
   - **Movies: theme park / merchandise revenue stream** — a real gap from the brief ("theme park/merchandise opportunities, Universal licensing deals... genre determines theme park eligibility") that had no cashflow window at all before today. `MovieProject.theme_park_value()`: a hard eligibility gate (`THEME_PARK_ELIGIBLE_GENRES = {Action/Tentpole, Sci-Fi/Fantasy, Animated}` — zero for everything else, not a smaller fraction), sized at `THEME_PARK_REVENUE_RATE` (3%) of domestic box office, landing in `windowed_cashflows()` at 30 months out (later than the library longtail's 24 — attractions and merchandise lines take real time to develop and license). Flows automatically into `total_revenue()`/`npv()` and the Greenlight-phase bear/base/bull preview since both already build off `windowed_cashflows()`; surfaced as its own line in the Results waterfall when nonzero.

8. **Overall leaderboard, Greenlighting → real roster wiring, AI pitch generator** — three more items from the same follow-up conversation.
   - **Overall leaderboard** — new `get_overall_leaderboard()` (`utils/game_state.py`), summing each team's OFFICIAL score across Oxygen/Bravo/Peacock only (Movies deliberately excluded, per explicit user request — its own separate track). A team missing a network isn't zero-padded; `networks_completed` just shrinks. New "🌐 Overall" tab, first in `pages/leaderboard.py`'s tab bar, with a matching podium/full-rankings treatment (`_render_overall_tab`) showing a per-network score breakdown per row.
   - **Greenlighting now actually adds shows to the real roster** — closes the foundational gap flagged (not built) in item 6 above. `pages/greenlight.py` gets a real "🎬 Greenlight This Show" action: creates a `Show` (new shows start at `ip_score=40`, just above the ~33 flat-maturation threshold so they trend mildly upward rather than assuming instant franchise value), appends it to the active network's roster list, deducts the unescalated production cost from `ss.level_budget` immediately (not refunded on Redo — same posture as Research fees), and is capped at `MAX_NEW_SHOWS_PER_YEAR = 3` (`utils/game_state.py`) — matches real network development slates (a handful of new titles per season against a 20-40 show base); budget already gates spending on top of this. Real care taken on undo semantics: `_remove_greenlit_shows()` (`pages/simulation.py`, new pure helper, tested) strips a show back out of the roster on **Redo This Year** (this year's additions only, budget not refunded) and **Restart This Level** (the whole level attempt's additions) — without this, a redo/restart would leave phantom shows in the roster that the reset logic elsewhere never knew to touch. Shows greenlit during a *passed* level correctly persist through **Advance to Next Network** (same as any pre-existing show).
   - **AI pitch generator** — complements the existing AI pitch grader (which only judges a pitch the student already wrote). New `ShowPitchIdea` model + `generate_show_pitch()` (`utils/ai_grading.py`) proposes an original concept (name, genre, 2-3 sentence pitch, suggested episode count/cost/rating/streaming-appeal) for a student stuck on ideation; a "🤖 Get an AI Pitch Idea" button in `pages/greenlight.py` pre-fills the concept-builder form via `st.session_state[key]` + rerun (the standard Streamlit pattern — set the widget's keyed session-state value before it's instantiated on the next run) rather than trying to mutate an already-rendered widget in place. Same BYOK/no-shared-key posture as the existing grader.

9. **Level Notables** — per-team plain-language playthrough highlights, computed once at Submit time from `yearly_log` (not the live roster, which cancellations/new-greenlights can have since changed). `compute_level_notables()` (`utils/game_state.py`) returns `best_year` (highest-margin year), `most_improved` (last year's margin minus first year's), `consistency_score` (0-100, derived from margin stdev across the level), and `diversity_trend` (first-year genre HHI minus last-year — negative means the slate diversified; `None` if a year had no active shows to measure). Depends on last session's genre-per-show-row tracking in `_compute_year()`. Stored as `record_attempt()`'s new `notables` field (own top-level key, same reasoning as `slate_summary` — `details` is iterated wholesale by the Leaderboard's badge rendering). **Movies-track equivalent**: `compute_movie_notables()`, wired into `pages/movies.py`'s Submit handler — `best_cycle`, `most_improved`, `consistency_score` map directly, but `genre_variety` (count of distinct genres across the 3 cycles) stands in for `diversity_trend` since a single movie-cycle is always one genre (HHI trivially 1.0, so a per-cycle *trend* isn't a meaningful signal the way it is for the TV side's larger annual rosters).
   - **Student-facing "Your Level Notables" panel**, added at the Complete phase of both `pages/simulation.py::_complete()` and `pages/movies.py::_complete()` (renamed "Your Slate Notables" there) — visible regardless of submission, computed live off the current `yearly_log`/`movie_log` so it stays current through Redo/Restart (distinct from the frozen copy `record_attempt()` stores). Four cards (Best Year/Cycle, Most Improved, Consistency, Shows Greenlit/Genre Variety) plus a one-line genre-diversity caption on the TV side. Verified with new AppTest smoke tests (`tests/test_simulation.py`, `tests/test_movies_page.py`) that seed `session_state` directly to the `"complete"` phase before the first `.run()` — sidesteps the documented AppTest + `st.rerun()` limitation, which only bites on a *second* interaction after a phase transition, not a single no-interaction run. 111 tests passing.
10. **Leaderboard: Attempts-Used + Notables badges** — closes out the Level Notables arc. Each full-rankings row in `pages/leaderboard.py::_render_board_tab` now shows a live `{attempts_used}/{MAX_ATTEMPTS} used` count via `get_attempt_count()` next to the pass/fail badge — deliberately live rather than reading `entry["attempt"]` (which is always 1 on the official row the leaderboard ranks by), so it reflects retries made *since* the official submission. New `_format_notables_badges()` renders whichever `notables` fields are actually present as a badge row underneath the score-details badges — handles both shapes (`best_year`/`diversity_trend`/`shows_greenlit` for TV, `best_cycle`/`genre_variety` for Movies) and empty/missing `notables` (pre-2026-07-27 entries) by simply rendering nothing. Same "renders whichever fields are present" approach as `_format_slate_item`. New `tests/test_leaderboard_page.py` — unit tests for the badge formatter plus one AppTest smoke test seeding a real `leaderboard.json` entry with `notables` and asserting the page renders with no exceptions. 117 tests passing.
    - **Level Notables arc is now fully closed**: computed → stored → shown to the student at Complete → surfaced on the Leaderboard.

11. **Decisions-flow redesign: both games are now a single scrolling page** — the request from earlier this session ("students should have prompts that force them to make decisions as they scroll down... then click simulate at the bottom"), previously deferred as its own design pass.
    - **TV/Streaming** (`pages/simulation.py`): the old 4-step Back/Next wizard (Financing → Renewal → Greenlighting → Scheduling, gated by `ss.decision_step`) is gone. `_decisions()` now renders all four sections unconditionally, top to bottom — each is a self-contained module (`pages/renewal.py`, `pages/greenlight.py`, `pages/schedule.py`) that already read/wrote `st.session_state` directly with no cross-module widget-key collisions, so none of those three files needed to change. A quick-jump anchor nav sits at the top; the "Expected This Year" P&L card (previously a persistent right-hand column, now full-width) and a single `▶ Simulate Year N` button sit at the bottom. `_step_bar`, `STEP_LABELS`, and `ss.decision_step` are deleted outright (`app.py` and every `_complete()` reset site cleaned up too) rather than left as dead state.
    - **Movies** (`pages/movies.py`): the old Greenlight → Release Strategy two-click-through collapses into one `_decisions()`. `ss.movie_phase` shrinks from `(greenlight, release, results, complete)` to `(decisions, results, complete)`. The per-card "Choose `<strategy>`" buttons are unchanged (still a live selection within the same page); Results stays its own separate dramatic-reveal page, untouched, matching the existing "neither draw is known to the student until this exact moment" design language.
    - **Two real, previously-undetected production bugs found and fixed while rewriting this** — direct evidence for why the "no real human click-through" gap (below) matters: (1) `pages/simulation.py::_decisions()` referenced a bare `net` name that was never passed in as a parameter — clicking "End Year" would have raised `NameError` for every single team, on every attempt, on the TV/Streaming side, forever. Nothing had ever exercised that click path in a test. (2) The End Year handler's `ss.mkt_budget = mkt` "sync" line, harmless under the old wizard (Financing's slider was never mounted in the same run as the End Year button), now raises `StreamlitAPIException` under the merged page (Financing's slider *is* always mounted) — removed as dead/harmful now that the slider's own `key=` binding keeps `ss.mkt_budget` current on every run.
    - New regression tests exercise the actual click path for both games (`tests/test_simulation.py`, `tests/test_movies_page.py`) — a single click that causes a phase transition is the *first* interaction in a fresh AppTest session, not a second one after an earlier transition, so it's inside the documented AppTest + `st.rerun()` safe zone. This is exactly the click path that caught bug (1) above.
    - **Follow-up pass, same day**: extended click-path coverage to every other newly-always-mounted interactive element on the merged pages, since finding two bugs on the first click path made the rest of the page suspect too — TV's Research button, Greenlight-This-Show button, Redo This Year, Restart This Level, Advance to Next Network; Movies' Choose-`<strategy>` buttons (tested at Cycle 3, where windowing unlocks). One more bug found, this time in the test suite itself, not the app: an early version of the Redo-This-Year test used synthetic `yearly_log` show-rows missing a `"variance"` field that `_results()`'s Rating Movers section requires — fixed the fixture. No further app-code bugs turned up; Restart/Advance were low-risk to begin with since this rewrite only deleted a `decision_step` line from each, not restructured them. 129 tests passing.
    - The two bugs just found are direct evidence for the top "Still open" item below — a real human/browser pass would very likely still turn up things AppTest structurally cannot catch (rendering, layout, visual glitches).

**Still open:**
- **No real human/browser click-through has ever happened, on Day 1 or Day 2, on any feature built across any session** — the single most important gap. Reinforced 2026-07-27: two real production bugs (see item 11 above) were sitting in the TV/Streaming Decisions page's Simulate-button handler, undetected until a regression test finally exercised that exact click path. **Attempted today, blocked twice**: (a) the live Streamlit Cloud deployment sits behind a `share.streamlit.io` login wall (see next bullet) — no credentials available to get past it; (b) tried to drive a real local browser instead — Claude in Chrome extension wasn't connected, and no real-browser automation library (Playwright/Selenium) is installed in the Anaconda env, so a headless fallback would've meant downloading ~300MB of browser binaries first. Asked; user chose to defer rather than have that installed right now. As a partial substitute, extended `AppTest` click-path coverage significantly (see item 11's follow-up above) — real, but structurally still not a substitute for actual rendering in a real browser. **Next session: either connect Claude in Chrome, or explicitly approve a Playwright install, before attempting a real click-through.**
- **Live deployment access-gated, status still unconfirmed** — https://comcastsimulator-f3riawhvhbihgap5qh2yzm.streamlit.app/ now redirects to a `share.streamlit.io` viewer login wall rather than showing the app or an error directly (confirmed via `WebFetch` 2026-07-27). This could mean the app's sharing setting is "invite-only" rather than public, or it could be unrelated to the still-unconfirmed `ImportError` fix from 07-22 — can't tell which without logging in. Whoever owns the Streamlit Cloud account should check the app's viewer-access setting and confirm it loads.

## History — 2026-07-24 end of session (superseded by 2026-07-27 above)

**What happened today:**
1. Restructured the quarterly Decisions phase (`pages/simulation.py`) into **Financing → Renewal → Greenlighting → Scheduling**. Renewal/Greenlighting/Scheduling reuse `pages/renewal.py`/`greenlight.py`/`schedule.py` verbatim — all three were fully built in earlier sessions but never actually imported or called from anywhere in the app, so students could never reach them despite the math and UI already existing. Financing (new revenue-stream breakdown — ad vs. distribution revenue — ahead of the existing marketing slider and cancel-shows decision) stays open by default; Renewal/Greenlighting/Scheduling sit below it.
2. Real bug caught mid-session: the original plan wrapped Renewal/Greenlighting/Scheduling in `st.expander`, which broke — `renewal.py` and `schedule.py` each already open their own internal `st.expander`, and Streamlit disallows nesting one expander inside another. Fixed by using `st.tabs` for the outer grouping instead; no changes to any of the three reused files' internals.
3. Sidebar cut down to nav-only. Removed the "Budget Allocation" marketing slider (duplicated the Decisions-phase slider; the Decisions-phase one is now the single source of truth). Movies (Day 2) moved from a sub-tab nested under whichever TV network happened to be active to its own peer button in the sidebar's network selector, next to Oxygen/Bravo/Peacock — `app.py`'s main content now branches on `ss.active_network == "movies"`.
4. Verified via headless `streamlit.testing.v1.AppTest`, run against the Anaconda install (per the "Real environment gotcha" note further down) — team registration, Oxygen's full Decisions phase (all four new sections, including the tabs' own internal expanders), and the Movies sidebar button all execute with zero exceptions.
5. Wrote an MBA-facing overview of the whole tool as a polished `.docx`. Not part of this repo; a standalone deliverable generated with `python-docx`, grounded in this doc's JTBD/mechanics sections rather than re-derived.

**Second pass, same day** — Zach Schlessel's feedback (quarters→years, real performance-linked budget, paid research, title-risk check, 3 charts), then a QA pass and a rename/nav restructure per the user:
6. Implemented quarters→years, real performance-linked budget, paid Research, title/IP legal-risk check, and 3 additive charts — see "Real mechanics built 2026-07-24" and the Zach Schlessel feedback section further down for full detail. Committed to git (commit `5cb039d`) at the user's request, not pushed anywhere.
7. QA pass caught two real bugs from the quarters→years rewrite: a dead `annual_budget` import left in `simulation.py`/`renewal.py`, and `app.py`'s `LEVEL_BRIEFS` "Suggested Order of Play" text still referencing pre-refactor tab names. Both fixed. Also deleted `pages/portfolio_v2.py` (confirmed dead, user chose to delete just this one — `finance.py`/`forecast.py` kept as-is, still orphaned but potentially useful later).
8. **Renamed CableOS → VideoOS** throughout code/docs, per the user (the tool now spans TV/Streaming + Movies, "Cable" undersold it).
9. **Sidebar restructured to 3-way top-level nav** (Leaderboard / TV-Streaming / Movies) — see "App structure" below. Added a "Choose Your Simulation" landing screen shown once after registering.
10. MBA overview `.docx` on the Desktop rebranded and content-refreshed — `VideoOS - MBA Overview.docx` (old `CableOS - MBA Overview.docx` removed). Not a text swap: rebuilt to describe the annual engine (4 years/level, was quarters), performance-linked budget, paid Research, title/IP risk check, and the 3-way TV/Movies/Leaderboard nav — the original was written before any of that existed.

**Third pass, same day** — UX rework of the Decisions phase per user feedback ("it's not clear students are being guided from decision to decision", "I want students to see previous year's P&L", "click each show and see a schedule populate underneath"):
11. **Guided 4-step wizard** replaces the old "Financing always-open + free-form tabs" layout — one step's content shown at a time (Financing → Renewal → Greenlighting → Scheduling), explicit numbered step tracker, Back/Next navigation, step 4's Next becomes End Year. New `ss.decision_step` state, reset to 1 at every year/level boundary (new year, redo year, network switch, advance, restart). Fixed a real staleness bug while doing this: the marketing slider previously only synced to `ss.mkt_budget` at End Year, so other pages reading it mid-year saw a stale value — now `key="mkt_budget"` binds the slider directly to that session-state field, always live.
12. **Last Year's P&L recap** — a pinned strip at the top of Decisions, starting year 2, showing the prior year's actual revenue/cost/OCF/margin. Previously this info vanished the moment a student advanced past Results.
13. **Renewal rebuilt as show cards** (`pages/renewal.py`) — the old column-of-selectboxes table replaced with a grid of per-show cards (rating, projected OCF, IP score, Renew/Watch/Cancel selector, and a premiere-month control that writes directly to the `Show` object's `air_month`). Below the grid, a live "This Year's Schedule" mini-view (12 month columns, count of premieres per month, hover for names) updates in real time as premiere months change — the "select a show, see a schedule populate" interaction the user asked for. Everything else in `renewal.py` (Budget Bridge, Genre Decay chart, paid Research, IP-value scatter, full data table) is untouched.
14. **Real, newly-confirmed instance of the existing AppTest limitation** (same class already diagnosed for `movies.py` in `tests/test_movies_page.py` — button handlers calling `st.rerun()` corrupt AppTest's widget-state tracking across a transition, not a real app bug). Adding explicit `key=` to `greenlight.py`'s and `schedule.py`'s previously-unkeyed widgets (a real, worthwhile fix regardless) shifted but didn't eliminate it — chaining several step transitions in one `AppTest` script (especially through Renewal's new 40-widget card grid) reliably crashes the harness, even though every individual step and interaction was verified correct in isolation (cards render, decisions persist, premiere months mutate the real Show objects, the mini-schedule updates, step counters increment/reset correctly). Did not redesign the wizard to dodge this, same reasoning as the existing `movies.py` precedent — would be a real UX downgrade for a test-tool artifact. **This makes a real human browser click-through of the full wizard flow (all 4 steps, multiple years) the single most important thing to do before using this with students** — more so than anything else flagged in this doc, since it's the most interaction-dense part of the app and the piece verified least completely by AppTest.

**Still open, carried over from 2026-07-22 below — neither has changed:**
- **No real human browser click-through has ever happened**, on Day 1 or Day 2. AppTest confirms today's restructure executes without exceptions — that's meaningfully weaker than an actual click-through (visual layout, whether Tailwind renders, whether the four new Financing/Renewal/Greenlighting/Scheduling sections are actually usable in one page rather than just import-clean). **Do this before using any of today's changes in front of students.**
- **Whether the Streamlit Cloud deployment survived the `ImportError` fix from 07-22 was never checked** — do that first, since it's independent of and predates today's local changes.

## History — 2026-07-22 session status (superseded by 2026-07-24 above)

**Live app**: https://comcastsimulator-f3riawhvhbihgap5qh2yzm.streamlit.app/ (Streamlit Community Cloud, auto-redeploys on every push to `main`). **Status unconfirmed** — it threw an `ImportError` on first deploy, a fix went out in the last commit of the day (`31109dd`), but nobody has checked whether the redeploy actually fixed it. **Do that check first, next session.**

**What happened today, roughly in order:**
1. Recovered CableOS's original design brief from an untracked file in a stale duplicate clone, formalized into this doc with JTBD for both student and instructor.
2. Designed and built the entire **Day 2 module** ("Universal Pictures" — movie/theatrical economics: NPV/IRR under variance, release-strategy tradeoffs, award season) — financial engine, turn-based UI, and tests. Caught and fixed a real calibration bug (unrealistic NPV, broken IRR) before shipping, and a real Streamlit deprecation (`components.v1.html` → `st.iframe`).
3. Set up Tailwind CSS (Play CDN, since there's no build pipeline here).
4. Diagnosed (not worked around) a genuine Streamlit AppTest testing-harness limitation — see `tests/test_movies_page.py`'s docstring.
5. **Consolidated the sidebar** per user feedback ("too many options for a simulation") — cut two dead sliders and a stale checklist, removed the Year control.
6. **Fixed a real crash** (`AttributeError: st.iframe`) the user hit running it locally — root cause was two Streamlit versions on their machine at different paths; fixed with a runtime capability check instead of assuming one version.
7. Fixed a theme mismatch (`.streamlit/config.toml` was light while the whole app is dark) and removed a stray duplicate config file.
8. **Built multi-school/multi-class leaderboard scoping** — a team's real identity is now `(school, class_section, team_name)`, not just a name, so two schools both having "Team Alpha" can't collide. Added a scope selector (My Class/My School/All Schools) and a school-vs-school comparison view. Added a 👑 crown icon for #1.
9. User **deployed the app live** to Streamlit Community Cloud (see URL above) and hit an `ImportError`.
10. Diagnosed as a likely Python-version mismatch — `utils/game_state.py`, `utils/models.py`, and `utils/charts.py` used modern `list[...]`/`dict[...]` type-hint syntax (needs Python 3.9+) without the `from __future__ import annotations` safety net, and had only ever been tested against a newer local Python than Cloud might be running. Fixed all three (commit `31109dd`) — **but this diagnosis was made from a redacted error message, not the real traceback, so it's a best-evidence guess, not a confirmed root cause.**

**Next session, in order:**
1. Check the live URL above. If it works, move on. If not, get the *full* (non-redacted) log from the app's "Manage app" panel — that's the one piece of information this session never had access to.
2. Once deployment is confirmed working: a real human click-through of Day 2's full flow (Greenlight → Release → Results ×3 → Complete → Submit) — still nobody has done this, in a browser or otherwise, all session (Claude in Chrome never connected). Also confirm Tailwind actually renders.
3. See "Working list — next up" below for smaller remaining items (award-season variance dimension follow-ups, etc.) — mostly already closed out.

## Premise

It's 2012. Linear TV's growth era is ending — subscribers are cutting the cord, Netflix is spending aggressively, and every network still runs its own siloed P&L. The student is the General Manager of a network, responsible for the show slate, the budget, and Operating Cash Flow (OCF) — with the long game being survival into the streaming transition.

## Jobs To Be Done

**Student (player)**
- When I'm handed a portfolio of shows I didn't choose, I want to quickly tell cash cows from dogs, so I can decide what to renew, cancel, or fund without re-deriving the whole P&L by hand.
- When I have to decide whether a new show goes on linear or streaming, I want a side-by-side P&L (ad revenue/CPM vs. subscriber LTV) built for me from a few inputs, so I can reason about the actual tradeoff instead of guessing.
- When I schedule a show's premiere date, I want to see the cash-timing consequence of a late-month launch, so I understand why amortization timing — not just total cost — matters.
- When I clear a network, I want a transparent score breakdown (not just pass/fail), so I know which specific decisions helped or hurt.
- When I fail a level, I want a bounded number of practice retries before advancing anyway, so one bad first attempt doesn't block the rest of the course.

**Instructor**
- When I run this in a live class, I want zero student PII stored anywhere, so I stay FERPA-compliant without having to vet the tool myself.
- When multiple teams play at once, I want a shared, tamper-resistant leaderboard keyed to the *first* attempt, so grading reflects real decisions, not the best of unlimited retries.
- When I teach a concept (BCG matrix, HHI diversification, amortization timing, LTV/CAC), I want it available as in-app reference content, not just something I lecture separately, so students can check the theory against their own numbers mid-game.
- When a level's difficulty needs tuning, I want the pass thresholds and scoring weights centralized in one place, so I'm not hunting through UI code to rebalance the game.

## Core financial mechanics

Grounded in `utils/models.py` (constants and formulas) and `utils/game_state.py` (progression/scoring).

### Amortization — the central teaching mechanic
Content cost is spread over an amortization window, paid monthly starting the month a show airs (ASC 926-style):
- **Oxygen**: 3-year (36-month) curve, ~$250–400K/episode — cheaper content, more breathing room.
- **Bravo**: 12-month curve, ~$650–900K/episode (reality) — costs hit harder each year, 5% escalation on renewal.
- **Peacock (SVOD)**: 36-month curve — spend upfront for deferred subscriber LTV instead of ad revenue.

**The premiere-day cash trough** (`Show.premiere_day_analysis()`, `pages/schedule.py`): a show launching March 30 absorbs a full month's amortization payment against only ~2 days of ad revenue. This is implemented essentially verbatim from the original spec, down to the March 30 example.

### Revenue model
- **Ad revenue**: `rating × $7M/rating-point × marketing lift × cord-cutting decay` (`Show.ad_revenue`).
- **Distribution (affiliate) revenue**: `cable subscribers (eroding 3%/yr) × $0.35/sub/month × 12 × escalation (capped Y5)` (`distribution_revenue()`) — the "dummy distribution revenue off subscriber count" from the original spec, now a real formula.
- **Marketing ROI**: each $1M of marketing spend lifts rating by ~1.5% (`MKT_ROI_PER_M`), split evenly across active shows.

### Green Light model (`pages/greenlight.py`, `greenlight_linear`/`greenlight_svod`)
The core strategic decision from the original spec — "do you put this show on linear or Peacock?" — fully built out:
- Side-by-side P&L: linear (cost, ad revenue, marketing, OCF, 12-month amortization) vs. SVOD (cost, subscriber lift, 3-year LTV, 36-month amortization).
- A crossover chart showing the month cumulative SVOD LTV overtakes linear's cumulative ad revenue.
- A rating × episode-cost sensitivity table (profitable vs. cancel, color-coded).
- Marketing-spend ROI comparison across both platforms.
- In 2012 (low year), linear wins on immediate cash; by year 7+, SVOD LTV starts winning — matches the original "should change over time" note.

### Scheduling / forecasting
An hourly viewership index (`HOURLY_INDEX`, 24 buckets peaking 8–10pm) simplifies the "every hour gets a forecast" idea from the original spec into a ratio system tractable for students, used in `pages/schedule.py` and `pages/forecast.py`.

### 10-year full-portfolio simulation (`pages/forecast.py`, `ten_year_sim()`)
Models the full arc: Oxygen alone (Y1–3) → + Bravo (Y4–7) → + Peacock (Y8+), tracking ad revenue, distribution revenue, cost, G&A, OCF, and cable sub erosion year over year.

## Progression & scoring

Three sequential levels — **Oxygen → Bravo → Peacock** — each independently scored. **Each level is now 4 real years (2026-07-24), not 4 quarters within a frozen Year 1** — see "Annual turn engine" below.

| Network | Pass threshold (OCF margin) | Note |
|---|---|---|
| Oxygen | 12% | Cheaper content, longer amortization — more forgiving |
| Bravo | 15% | Core cash-cow economics, tighter budget |
| Peacock | 10% | SVOD early days — thinner margins accepted |

Score is a weighted composite (`compute_score`, `SCORE_WEIGHTS`), not just OCF:
- OCF margin — 35%
- Average show ROI — 25%
- Genre diversification (inverse HHI) — 15%
- Marketing efficiency — 15%
- Sound renewal decisions — 10%

**Attempts**: first attempt is locked as the official leaderboard score. Up to `MAX_ATTEMPTS` (3) total; a team can advance past a failed level once they've passed on any attempt *or* completed at least one retry — no free pass on a single first-try fail, but no permanent block either.

## Deliberate simplification vs. the original spec

The original brief described continuous multi-year play — "prove yourself over 3–5 years, then earn a second network; launch streaming in year 6, 8, or 10." The built game instead uses **pass/fail attempts** to gate advancement between networks, rather than a single unbroken 10-year playthrough. This is an intentional compression for classroom time constraints, not a missed requirement — the 10-year full-portfolio arc still exists as a standalone forecasting exercise (`pages/forecast.py`), just decoupled from the level-gating mechanic itself.

**Partially reconciled 2026-07-24**: each level is now a genuine 4-year run (see "Annual turn engine" below) rather than a single frozen year — closer to the original "prove yourself over 3-5 years" framing than the quarterly engine it replaced, though still a bounded-attempt gate per level rather than one continuous playthrough across all three networks. Zach Schlessel's 2026-07-24 feedback pushes further in this direction (see his feedback section) — worth revisiting whether a continuous cross-network arc is the eventual target.

## Annual turn engine (2026-07-24 — replaces the quarterly engine)

Rebuilt `pages/simulation.py` from a quarterly loop (4 quarters inside a frozen "Year 1") to a real annual loop — `ss.year` is now the actual turn counter (1→`YEARS_PER_LEVEL`=4) driving every financial formula, not a fixed value the sidebar used to (and could no longer) change. Direct response to Zach Schlessel's "change from quarters to years" feedback, scoped specifically to the turn-cadence structural change — see his feedback section for the rest, still backlog.

- **Financial math simplified, not just relabeled**: cost/revenue no longer need the `/4` quarterly-slice bookkeeping (`monthly_amort(year)*3`, `ann_mkt/4`, etc.) — a turn is a full year now, so `Show.annual_amort_expense()` / `ad_revenue()` / `distribution_revenue()` are used directly. Removes a real inconsistency the quarterly engine had: revenue/cost were divided into quarters while `year` stayed frozen at 1 all session, so escalation/decay never actually progressed within a level; now each turn genuinely reflects that year's economics.
- **Renewal is now the actual cancellation mechanism.** Wiring Renewal into the Decisions phase earlier the same day only made it *visible* — its Renew/Watch/Cancel choices were written to `ss.renewal_decisions` but never mutated `ss.cancelled_shows`. With annual turns, Renewal is where Zach's "Order of Play" puts the cancellation decision, so its Cancel choices now drive `ss.cancelled_shows` directly at year-end. Financing's old standalone "Decision 2 — Cancel Shows" quick-multiselect was removed as redundant.
- **Cancellation penalty**: mid-year cancellations pay 25% of that year's amortization as a sunk-cost penalty (the annual-cadence equivalent of the old quarterly engine's "50% one-quarter penalty" — same intuition, rescaled).
- Session-state field renames: `ss.sim_month` (quarter-within-year counter) removed — `ss.year` serves this role directly now. `ss.monthly_log` → `ss.yearly_log`; each entry's `"quarter"` key → `"year"`.
- Verified via a full `AppTest` run through all 4 years of Oxygen (Decisions→Results ×4 → Complete → Submit → Advance to Bravo) — no exceptions, real score computed (88.8/100, passed), `ss.year` correctly reset to 1 on advancing to Bravo.
- **Not yet done**: a real human browser click-through of the new annual flow (same standing gap as the rest of this doc — AppTest confirms it executes, not that it's legible/usable on screen).

## App structure

**Sidebar nav restructured 2026-07-24 (second pass, same day)** — three peer top-level sections: 🏆 Leaderboard, 📺 TV/Streaming, 🎬 Movies (`ss.active_section`). Previously Leaderboard was a tab nested inside whichever of TV/Movies was active (duplicated in both), and Movies was a peer of the Oxygen/Bravo/Peacock network buttons rather than a peer of TV itself. The Oxygen/Bravo/Peacock selector is now a secondary selector shown only when TV/Streaming is the active section (`ss.active_network`, unchanged mechanics — lock icons, attempt badges, official scores). A new "Choose Your Simulation" landing screen (`ss.active_section is None`) appears once right after registering, with TV/Streaming and Movies as two cards plus a Leaderboard shortcut; picking a section persists it in the sidebar for free switching afterward.

**4th top-level nav button added (2026-07-27)**: 🏠 App — the landing screen above existed but had no way back to it once you'd picked a section (only "Change Team" got you there, by deregistering). Now `ss.active_section == "app"` (and the initial `None` state) both route to it; the nav button highlights correctly in both cases.

| Section | File | Purpose |
|---|---|---|
| 🏠 App | (landing screen, in `app.py`) | "Choose Your Simulation" cards (TV/Streaming, Movies) + Leaderboard shortcut. Reachable any time via the sidebar now, not just once after registering. |
| 🏆 Leaderboard | `pages/leaderboard.py` | Standalone top-level section as of this pass (was nested tabs, duplicated per-section). Already covered TV networks + Movies internally via its own tabs (`_render_board_tab`, `MOVIES_INFO`) — no wrapping needed, `render()` takes no args and reads `ss` directly. |
| 📺 TV/Streaming → Simulation (tab) | `pages/simulation.py` | Annual turn engine — Decisions → Results × 4 years per network, then submit for score. Financing (revenue-stream breakdown, marketing spend) is the always-open anchor; cancellation lives in the Renewal tab. |
| 📺 TV/Streaming → Renewal (sub-tab) | `pages/renewal.py` | Renew/Watch/Cancel decision matrix, IP-value vs. OCF tradeoff, budget waterfall, paid Research. |
| 📺 TV/Streaming → Greenlighting (sub-tab) | `pages/greenlight.py` | Linear vs. SVOD P&L builder for a new show concept, title/IP legal-risk check. |
| 📺 TV/Streaming → Scheduling (sub-tab) | `pages/schedule.py` | Premiere-day cash trough, monthly amortization grid, scheduling optimizer. |
| 📺 TV/Streaming → P&L / OCF (tab) | `pages/finance.py` | **Wired in 2026-07-27** (previously orphaned). Full income statement, monthly revenue/cost trend, distribution-revenue calculator, revenue-per-rating-point benchmarks — a fuller view than Financing's condensed summary. Reads the currently active network's shows/year/marketing spend from `ss` directly, no new state needed. |
| 📺 TV/Streaming → 10-Yr Forecast (tab) | `pages/forecast.py` | **Wired in 2026-07-27** (previously orphaned). Scenario tool spanning all three networks — adjustable cord-cut rate, budget growth, Bravo/Peacock launch year — independent of the active network's actual progress, so it lives as a tab rather than being tied to `ss.active_network`. |
| 📺 TV/Streaming → Theory (tab) | (in `app.py`, `THEORY_CONTENT`) | BCG matrix, HHI diversification, cord-cutting S-curve, amortization, LTV/CAC. |
| 🎬 Movies | `pages/movies.py` | Universal Pictures — Day 2. Own top-level section, own Theory tab, independent of TV network progress. |

`pages/portfolio_v2.py` — superseded by `simulation.py`'s turn engine (per commit `24ce9c9`), confirmed nothing imported it, **deleted 2026-07-24**.

## Day 2 — Movie/Theatrical Component ("Universal Pictures")

Scoped 2026-07-22. Audience is **MBA students** (same as Day 1) — this is not a simplified undergrad add-on, it should push real capital-allocation-under-uncertainty analysis. Universal Pictures is a real NBCUniversal division; theatrical economics (box office exhibitor splits, marketing spend front-loaded relative to and often exceeding production cost, theatrical/PVOD/streaming windowing, the Peacock exclusive-window strategy) are a genuinely different financial model than Day 1's TV amortization curves — good pedagogical contrast, not a redundant reskin.

**Why Day 1 (TV) comes first, and how it connects**: Day 1 teaches the steady-state building blocks — amortization timing, recurring P&L, portfolio thinking — through a forgiving, repeatable rhythm (20 shows, quarterly turns, retries allowed). Day 1's Green Light tab (linear vs. Peacock for a new show) is structurally the *same question* a movie studio asks — Day 2 doesn't introduce a new concept, it raises the stakes on one students already have: a portfolio of steady, amortized bets (TV) vs. one concentrated, front-loaded, fast-decaying bet (a movie).

**Duration: 6 years, structured as 3 movie cycles of 2 years each.** Revised from an initial 5-year guess — a real studio film runs 18–24 months from greenlight to release (pre-production/writing, the shoot, then post/VFX), so 5 years barely fit two full cycles. 6 years gives students **3 complete greenlight-to-release cycles**, enough to actually apply a lesson learned in Cycle 1 to Cycle 2 and 3 — the movie-industry equivalent of Day 1's "first attempt is official, retries are practice," but at the scale of a whole bet instead of a quarter.

- **Cycle structure (repeated 3×, Years 1–2, 3–4, 5–6)**:
  - **Year N (Greenlight/Production)** — the single-movie decision: budget tier, genre, talent, P&A commitment. Cash goes out; no revenue yet.
  - **Year N+1 (Release/Windowing)** — release-strategy decision (theatrical/day-and-date/platform), window-length choice, then actual box-office/streaming results resolve against the bull/base/bear scenario drawn for that title.
- **Portfolio complexity escalates across cycles**, not within Year 1 alone: Cycle 1 is a single film in isolation (learn the mechanics). Cycles 2–3 layer in slate effects — multiple concurrent titles, cannibalization dynamics compounding as PVOD/streaming windows realistically shorten year over year (mirrors Day 1's Oxygen → combined-portfolio escalation, just compressed into 2 more cycles instead of network unlocks).

### Cost structure — the core contrast with Day 1
Both production budget (`P`) and P&A/marketing spend (`M`) are committed *upfront*, before any revenue visibility — unlike Day 1's monthly-amortized cost spread over a season. Total capital at risk = `P + M`.

### Revenue engine — windowed waterfall, not a steady monthly stream
- **Opening weekend** = `f(marketing awareness, star power, genre appeal, screen count)` — same diminishing-returns marketing curve as Day 1's `MKT_ROI_PER_M`, but the payoff concentrates into one weekend instead of a season.
- **Total domestic box office** = opening weekend × a **multiplier** (real industry term — "the film did a 2.8x"). This is where quality/word-of-mouth risk lives: marketing can buy an opening, not legs. Model the multiplier as the variable that differs across scenarios (see Variance below), not the opening weekend itself.
- **International box office** = domestic × a genre-dependent multiplier.
- Studio nets roughly half of box office (exhibitor split, front-loaded higher early in the run, declining over time) → **PVOD window** (premium rental, studio keeps the large majority of the transaction) → **Peacock exclusive streaming window** → **EST/home entertainment** → long-tail **library licensing**.

### NPV over IRR as the primary metric
Discount the windowed cash flows at a studio cost-of-capital proxy (~10–12%), subtract `P + M` upfront. NPV is the right unit — a single bet doesn't have a "margin," it has a return on capital at risk. Report IRR alongside it, since front-loaded-cost/back-loaded-revenue structures can swing IRR wildly — that swing is itself part of the lesson.

### Variance is graded, not hidden
Run bull/base/bear scenarios on the box-office multiplier (and optionally the international multiplier). Score partly on **NPV under the bear case**, not just expected NPV — the single biggest thing that separates this from Day 1's deterministic P&L, and the reason a real-options framing (staged investment; option to expand/abandon at each checkpoint — greenlight → production → marketing commit → wide vs. platform release) is legitimate here, not decorative.

### Cannibalization math — the direct extension of Day 1's Green Light tab
If a student picks day-and-date/streaming, apply a cannibalization discount to theatrical box office, but credit a dollarized "attributable subscriber value" (Peacock adds/retention tied to the title — same LTV logic Day 1 already applies to SVOD). Net comparison of *(theatrical-heavy NPV)* vs. *(streaming-heavy NPV, net of cannibalization)* is the actual decision, calculable rather than a gut call. Ground the release-strategy decision in real comparables (WarnerMedia's 2021 HBO Max day-and-date experiment, Universal's post-2020 shortened theatrical window with AMC) rather than an abstract slider.

### Composite score (parallel to Day 1's `compute_score`, different components)
- **Risk-adjusted NPV** (weighted toward the bear case) — largest weight, core financial outcome.
- **Capital efficiency** — box-office lift per marketing dollar, penalizing "just spend the max."
- **Strategic fit** — did the release-window choice maximize combined theatrical + streaming value net of cannibalization, vs. a naive "theatrical is always better" default.
- **Decision quality at checkpoints** — if built with staged real-options turns, grade whether the student's contingent choice matched what the interim signal (tracking/awareness data, opening-weekend actuals) actually implied.

**Pass/fail gate**: positive risk-adjusted NPV (not a fixed OCF margin %) — judged on whether the bet cleared its cost of capital under a reasonably conservative scenario.

**Not yet decided**: exact turn structure for the staged real-options checkpoints (biggest open design risk — Day 1's tabs are single-decision-per-page; Day 2 may need a sequential-stage UI closer to `pages/simulation.py`'s quarterly engine), and whether Day 2 shares the existing leaderboard/attempt system or stands alone.

## Frontend: Tailwind CSS

Added 2026-07-22 (`utils/styles.py::TAILWIND_INJECT`, wired in `app.py`). Streamlit has no bundler step and this repo has no Node/npm toolchain, so this uses Tailwind's **Play CDN** (browser-side JIT) rather than a compiled build pipeline — the right-sized choice for a classroom tool, even though Tailwind's own docs flag Play CDN as unsuited to high-traffic production (doesn't apply here).

**Real gotcha, worth remembering**: `st.markdown(html, unsafe_allow_html=True)` renders via `innerHTML`, and `<script>` tags inserted through `innerHTML` never execute in any browser — this is a DOM-level restriction, not Streamlit-specific. The correct injection path is `st.components.v1.html(...)`, but that renders inside a *sandboxed iframe* — so the injected script has to reach out via `window.parent.document` to attach itself to the actual app page, not just the throwaway iframe. `TAILWIND_INJECT` does this, with a dedup guard (`id="tailwind-cdn"`) since Streamlit re-runs the whole script on every interaction.

Theme extended (`tailwind.config`) to match the existing hand-rolled palette in `GLOBAL_CSS` (bg/surface/line/ink/gold/etc., DM Serif Display/DM Mono/DM Sans), so new markup can mix Tailwind utility classes with the existing design tokens rather than clashing with them. **Adoption is incremental, not a forced rewrite**: new pages/components (including Day 2) should reach for Tailwind classes first; the ~3,500 lines of existing inline-styled HTML across `app.py`/`pages/*.py` don't need migrating unless touched anyway.

## Final JTBD list (consolidated — Day 1, Day 2, and the frontend)

**Student (player)**
1. When I'm handed a portfolio of shows I didn't choose, I want to quickly tell cash cows from dogs, so I can decide what to renew, cancel, or fund without re-deriving the whole P&L by hand.
2. When I decide whether a new show goes on linear or streaming, I want a side-by-side P&L (ad revenue/CPM vs. subscriber LTV) built from a few inputs, so I can reason about the real tradeoff instead of guessing.
3. When I schedule a show's premiere date, I want to see the cash-timing consequence of a late-month launch, so I understand why amortization *timing*, not just total cost, matters.
4. When I clear a network, I want a transparent score breakdown, so I know which specific decisions helped or hurt.
5. When I fail a level, I want a bounded number of practice retries before advancing anyway, so one bad first attempt doesn't block the rest of the course.
6. **(Day 2)** When I greenlight a movie, I want to see risk-adjusted NPV across bull/base/bear scenarios, not one deterministic number, so I learn to evaluate a concentrated bet the way a real studio does.
7. **(Day 2)** When I choose a release strategy (theatrical/day-and-date/platform), I want the cannibalization-vs-subscriber-value tradeoff calculated explicitly, so the decision is analytical, not a gut call.
8. **(Day 2)** When I manage Cycles 2 and 3 of a multi-year slate, I want portfolio-level effects (compounding cannibalization, shrinking windows) to carry forward from my earlier decisions, so the exercise rewards sustained strategy, not just one good pitch.

**Instructor**
9. When I run this in a live class, I want zero student PII stored anywhere, so I stay FERPA-compliant without vetting the tool myself.
10. When multiple teams play at once, I want a shared, tamper-resistant leaderboard keyed to the first attempt, so grading reflects real decisions, not the best of unlimited retries.
11. When I teach a concept (BCG matrix, HHI, amortization timing, LTV/CAC, and — new for Day 2 — NPV/real options under variance), I want it available as in-app reference content, so students can check theory against their own numbers mid-game.
12. When a level's difficulty needs tuning, I want pass thresholds and scoring weights centralized in one place, not scattered through UI code.
13. **(Frontend)** When I extend the UI (Day 2 or otherwise), I want a real utility-class system available, so new pages aren't hand-rolled inline-style strings copied from old ones.

## Working list — next up

**Built 2026-07-22, same session as the design above:**
- **`utils/movie_models.py`** — full financial engine: `MovieProject` dataclass, opening-weekend/box-office/PVOD/subscriber-value/library formulas, `windowed_cashflows()`, `npv()`/`irr()`, `risk_adjusted_npv()`, `capital_efficiency()`, `strategic_fit_score()`, `compute_movie_score()`, `draw_actual_multiplier()` (seeded, reproducible-per-team-per-cycle continuous outcome draw via `np.random.triangular`), `nearest_scenario_label()`.
  - **Calibration matters, caught before shipping**: the first pass produced a $200M movie showing a $650M+ NPV and an IRR pinned at the 500% search ceiling for every scenario — both wrong. Root causes: (1) `opening_weekend()` stacked star-power and P&A boosts multiplicatively instead of additively, blowing up box office scale; (2) theatrical revenue was timed at 2 weeks post-release, and *annualizing* a return that concentrated that fast produces an absurd IRR even for an ordinary hit. Fixed by rebalancing the opening-weekend formula (verified against realistic $10-20K/screen benchmarks) and moving theatrical revenue recognition to the run's midpoint (~6 weeks). Re-verified with a real smoke test (see the numbers below) before building any UI on top of it — don't trust a financial model that merely runs without erroring.
  - Verified realistic output: a $200M tentpole (wide theatrical) now shows base-case NPV ≈ +$179M, bear ≈ +$44M, bull ≈ +$369M — a believable spread. A $25M indie (platform release) shows base ≈ -$1M, bear ≈ -$10M — appropriately marginal/risky. Day-and-date on the same tentpole in Cycle 3 drops NPV to negative (theatrical suppression not fully offset by subscriber value) — a real, teachable result, not an artifact.
  - `irr()` now distinguishes "never recovers capital" (`None`) from "true IRR exceeds the 500% search ceiling" (`float('inf')`, displayed as ">500%") from an actually-converged rate — it no longer silently returns a boundary value dressed up as a precise answer.
- **`pages/movies.py`** — the turn engine, mirroring `pages/simulation.py`'s Decisions→Results phase-state-machine pattern (confirmed as the right call, not just a superficial resemblance): Greenlight (concept + capital commit) → Release Strategy (side-by-side risk-adjusted NPV preview across wide/platform/day-and-date, extending Day 1's Green Light tab) → Results (actual outcome resolves against a hidden continuous draw, revenue waterfall chart, slate-so-far chart) → repeats for all `CYCLES_TOTAL` (3), then a Complete phase with the composite score breakdown and submission. Submission reuses Day 1's existing FERPA-safe leaderboard infra (`game_state.py::record_attempt`) under a new `"movies"` network key — no new persistence system needed.
- **`pages/leaderboard.py`** refactored — per-network tab body extracted into `_render_board_tab()` and reused for a new "🎬 Universal Pictures" tab (`MOVIES_INFO`, deliberately *not* added to `NETWORK_INFO`/`NETWORK_ORDER` so it can't leak into the sidebar's TV network selector). Without this, a submitted movie score would have been recorded but invisible anywhere in the UI.
- **`app.py`** — new "🎬 Movies (Day 2)" tab, positioned after Simulation and before Leaderboard. Fully independent of the sidebar's Active Network (Oxygen/Bravo/Peacock) selection — Day 2 doesn't read or depend on `ss.active_network`.

**Verified this session**: all new/changed files syntax-check and import cleanly; the financial engine was smoke-tested directly (function calls, not just "doesn't crash") through several scenarios until the numbers were realistic; the local server (`localhost:8511`) starts with no traceback on the full wiring.

**Not yet verified — needs a human in a real browser** (Claude in Chrome wasn't connected in this environment across this entire multi-session effort, repeated connection attempts all failed): a full click-through of Greenlight → Release Strategy → Results → 3 cycles → Complete → Submit, and confirming Tailwind actually renders. Please run through at least one full slate before trusting this in front of students.

**Also built same session, working the list further (2026-07-22):**
- **`tests/test_movie_models.py`** (+ `tests/__init__.py`, `requirements-dev.txt`) — 24 tests, all passing. Not just "does it run" checks: pins the tentpole/indie NPV examples above to realistic dollar ranges, asserts `irr()` correctly distinguishes never-recovers (`None`) / exceeds-ceiling (`inf`) / a real converged rate, confirms day-and-date nets out worse than wide theatrical for a big tentpole (and skips PVOD entirely), confirms the window shrinks per cycle and floors at 17 days, confirms the scenario draw is reproducible per team+cycle and genre-bounded, confirms scoring clamps to 0-100 and an empty slate scores zero/fails. This is the regression guard the original calibration bug should have had from the start.
- **Genre-differentiated variance** — `GENRE_VARIANCE_SPREAD` widens/narrows the bear-to-bull spread per genre around a shared base case (Horror 1.6x baseline spread — the textbook high-variance genre — down to Awards/Prestige 0.7x, a narrower specialty-audience range). `domestic_box_office()`, `draw_actual_multiplier()`, and `nearest_scenario_label()` all now resolve genre-adjusted bounds automatically — no UI changes needed, the Greenlight/Release preview cards already read genre-aware NPV ranges through the existing code path.
- **Screen-count sanity warning** (`pages/movies.py`) — soft warning (not a hard block, math still runs either way) when planned opening screens are wildly disproportionate to total capital committed (rough benchmark: 18 screens/$M), since a student could otherwise "game" a huge opening off a tiny budget in a way no real distribution deal would allow.
- **Real Streamlit deprecation caught and fixed**: `st.components.v1.html` (used for the Tailwind injection) is deprecated, flagged for removal after 2026-06-01 — discovered via a warning surfaced while import-checking the full app, not from documentation. Replaced with `st.iframe(TAILWIND_INJECT, height=1)`, Streamlit's own recommended successor, which auto-detects a raw HTML string the same way. One real gotcha: `st.iframe` rejects `height=0` outright (`StreamlitInvalidHeightError`) where the old API silently allowed it — `height=1` is the smallest valid value and is visually negligible.

**Also built (2026-07-22): `tests/test_movies_page.py`** using Streamlit's official `streamlit.testing.v1.AppTest` harness — runs `pages/movies.py::render()` headlessly (no browser) and confirms the Greenlight phase renders with zero exceptions, the expected widgets exist, starts at cycle 1 with an empty log, and the bear/base/bull preview actually shows.

**Real, conclusively-diagnosed limitation, not a bug in the app**: a full interactive click-through (Greenlight → Release → Results → 3 cycles → Complete → Submit) via `AppTest` was attempted and hit a genuine Streamlit 1.59.2 AppTest bug — any button handler calling `st.rerun()` (the pattern used consistently across this *entire* codebase: `simulation.py`, `app.py`, and `movies.py` all do `if st.button(...): ss.x = y; st.rerun()`) corrupts AppTest's widget-state tracking on the *second* interaction after a phase transition, raising a spurious `KeyError` for a widget from the *previous* phase that's no longer rendered. Proven via a minimal synthetic repro isolated from the real code: removing `st.rerun()` made an identical two-phase flow pass cleanly; adding it back reproduced the failure every time, regardless of click pattern (chained vs. separate `.run()`, held references vs. re-querying fresh, an extra "settle" pass — none of it helped). **Did not change `movies.py`'s `st.rerun()` usage to work around this** — that would make it inconsistent with the rest of the app (which presumably works fine in real browsers, where this AppTest-specific issue doesn't apply) for no real benefit. Full detail preserved in `tests/test_movies_page.py`'s module docstring so a future session doesn't have to redo this multi-hour diagnosis. Worth retrying the interactive tests if Streamlit is ever upgraded past 1.59.2.

**Award season / critical reception — built 2026-07-22.** A genuinely separate risk axis from box-office performance, not just another slider: a movie can open huge and get panned, or open modestly and find acclaim, so `draw_critical_reception()` uses its own independent seeded triangular draw (own genre-tuned `CRITICAL_RECEPTION_BOUNDS`, own seed offset from the box-office multiplier's draw) rather than deriving reception from the same outcome. Two real effects, both zero when `critical_score` is unresolved (planning-stage bear/base/bull previews correctly show *no* reception effect — a student can't know reviews in advance, and leaking that into the preview would be a spoiler):
- **Library/EST longtail scales 0.7x–1.8x** with reception — acclaimed films have real, enduring library value independent of theatrical performance.
- **Awards-season rerelease bump** — a new, genuinely separate cash-flow window (~11 months out, For-Your-Consideration-style limited rerelease), but *only* for `AWARDS_ELIGIBLE_GENRES` (Drama, Awards/Prestige) clearing `AWARDS_CONTENDER_THRESHOLD` (72/100). An action tentpole never gets this, no matter how well-reviewed — matches how the real industry actually works, not just "higher score = more money everywhere."

`risk_adjusted_npv`/`capital_efficiency`/`strategic_fit_score`/`compute_movie_score` all thread an optional `critical_score` through — the **final slate score now reflects the actually-resolved reception**, not just the hypothetical bear/base box-office scenarios it already accounted for. This closes a real gap that would otherwise have existed: the Results screen would have shown the awards bump, but the Complete-phase score would have silently dropped it since it reconstructs `MovieProject` objects from scratch rather than reusing the resolved outcome. Caught before shipping, not after. 7 new tests (`TestAwardSeason`) cover: action never gets the bump, drama below/above threshold, no-critical-score means no bump and unscaled longtail, acclaimed vs. panned longtail direction, draw reproducibility/independence from the box-office seed, and that final scoring only ever helps (never hurts) relative to being scored blind.

**Final state, end of day 2026-07-22**: 46 tests passing (35 on Day 2's financial engine/page, 11 on multi-school leaderboard scoping — see "Multi-school / multi-class leaderboard" below), full app import-checks cleanly against the actual Anaconda runtime (not just whatever Python a fresh shell happens to resolve to — see "Status as of" at the top of this doc for why that distinction mattered), local server starts with no traceback, app is live-deployed (status unconfirmed, see top of doc).

**Still genuinely open** (see "Status as of 2026-07-22" at the top for the full, current list — this one is superseded):
1. ~~The actual interactive click-through~~ — still true, still nobody's done it, tracked at the top of this doc now alongside the deployment check.
2. ~~Tailwind's actual rendering is unconfirmed~~ — same, tracked at the top now.

## Multi-school / multi-class leaderboard — added 2026-07-22

Team identity used to be just `team_name` (a free-text pseudonym), which meant two different schools — or two sections of the same school — both having a "Team Alpha" would silently share attempt history and leaderboard position. Fixed by making a team's real identity the triple **(school, class_section, team_name)**:

- **Registration** (`app.py`) now collects School (curated dropdown — Northwestern Kellogg / Indiana Kelley / "Other" free-text, so any school can use this without a code change) and Class/Section (free text) alongside Team Name. Same FERPA posture as team_name itself — self-reported classroom context, not tied to any student identity.
- **Every gating/attempt function** in `utils/game_state.py` (`get_team_attempts`, `get_official_score`, `get_attempt_count`, `can_advance`, `get_team_network_status`) now takes `school`/`class_section` and scopes matching on all three fields, not just team_name.
- **`get_network_leaderboard(network, school=None, class_section=None)`** — `None` (default) means unfiltered/cross-school; a value scopes down. This is the one function that directly satisfies all four of the user's requirements via different filter combinations:
  1. **Class-only leaderboard** → `school=X, class_section=Y`
  2. **Team-vs-team across schools** → unfiltered (`None, None`), now shown with school/class as a label on each row
  3. Same as #1 — a class leaderboard *is* the class-scoped view
  4. **School-overall leaderboard** → `school=X, class_section=None` (every class at that school, rolled together)
- **`get_school_rollup(network)`** — new aggregate function (avg score, pass rate, top score, team count per school) — the school-vs-school comparison view, satisfying the other half of #2.

`pages/leaderboard.py` exposes this as a **scope selector** (My Class / My School / All Schools, `st.radio`) controlling the per-network podium/rankings/chart, plus a standalone **School Comparison** section (table + bar chart) always showing every school side by side regardless of scope selection.

**Verified, not just wired up**: `tests/test_game_state_scoping.py` (11 tests) — same-name teams at different schools/sections genuinely don't collide (attempts, `can_advance`, leaderboard ranking all isolated correctly), filtered views exclude what they should, rollup aggregates and ranks correctly. 46 tests total now passing.

**Fun top-3 icons** (the lighter half of this request): 👑 crown for #1 (replacing a plain gold medal) with a looping CSS glow (`utils/styles.py::crown-glow`), silver/bronze medals unchanged for #2/#3.

## Running this for a real class — operational notes

**Homework/in-class split + `FREE_NAVIGATION`, 2026-08-07** — per user request, a real class structure: assign Oxygen as homework so students arrive familiar with the mechanics, then use one class period (~1hr) to work through both Bravo and Peacock, leaving the last ~25 minutes for discussion. That only works if a student can jump straight to Bravo, or straight to Peacock, instead of the normal sequential gate (pass, or fail-and-retry-once, to advance — `utils/game_state.py::can_advance`) blocking them behind whatever they did or didn't finish on Oxygen at home. New instructor secret `FREE_NAVIGATION` (`utils/game_state.py::_read_free_navigation`, defaults **off** — unchanged behavior for every deployment until an instructor opts in, see README.md) forces `locked=False` for every network in `get_team_network_status()` while leaving attempts/official scores/leaderboard ranking untouched — it only changes which levels are clickable in the Active Network selector (`app.py`), not how anything is scored. Mechanically safe to jump ahead: all three networks' show slates (`oxygen_shows`/`bravo_shows`/`peacock_shows`) are seeded for every team at registration regardless of play order, so Bravo/Peacock's cumulative `shows` list is complete whether or not Oxygen was actually played first.

**Driver/Follow Along live-state sync, 2026-08-07** — direct follow-up to the note below: a team spread across multiple laptops can now genuinely follow along together, without an account system. Registration gains a role radio (`app.py`'s `role_input_field`) — "🎮 Driver" or "👀 Follow Along" — stored as `ss.team_role`. The Driver plays for real; every teammate registers Follow Along under the *exact same* School/Class/Team Name (that triple is still the whole link, no accounts) and gets a read-only dashboard instead. New storage layer in `utils/game_state.py` (`save_live_state`/`load_live_state`, `team_state.json`, parallel to `leaderboard.json`) persists the Driver's in-progress decision state — shows, cancellations, renewal decisions, year, budget, etc. — keyed by the same (school, class_section, team_name) triple used everywhere else. `app.py` owns serializing its own `Show`/`SportsContract` objects and sets into/out of the JSON-safe snapshot those functions persist (`_build_live_snapshot`/`_apply_live_snapshot`) — deliberately dumb storage, same separation `record_attempt()` already has from `Show` objects.

Scoped to TV/Streaming only (Movies isn't wired in yet). Deliberately did **not** disable the ~27 live decision widgets across `simulation.py`/`renewal.py`/`greenlight.py` to build a Viewer mode — too much surface area to risk destabilizing the Driver's tested gameplay logic for a first cut. Instead, Follow Along renders a separate lightweight read-only summary (`app.py::_render_follow_along_summary` — year/phase/budget metrics plus a plain slate table) in place of the interactive Simulation tab, while reusing `render_finance()`/`render_forecast()` as-is (already pure display, zero changes needed there). Refresh is a manual "🔄 Refresh" button rather than an auto-polling timer, per explicit user choice — avoids a new dependency (`streamlit-autorefresh`) and continuous server load from every viewer's laptop; any button click reruns the whole script anyway, which re-reads the Driver's latest saved state at the top of the TV dashboard branch.

**Real bug caught building this**: the sync hooks were originally wired into what looked like "the registered TV dashboard" `else:` branch, but `app.py`'s actual dispatch has a separate `elif ss.active_section in (None, "app"):` branch for the post-registration home-cards screen — a student has to click "📺 TV / Streaming" in the nav row to ever reach the branch the sync hooks live in. Caught by a real AppTest click-through (`tests/test_driver_viewer_sync.py`) that initially asserted a saved snapshot right after registration and got `None` back — turned out to be a test bug (missing the nav click), not a feature bug, but it surfaced a second, real one along the way: `save_live_state()` fires on every Driver render (not just explicit submissions), so a handful of pre-existing AppTest smoke tests that register a team and render the dashboard were quietly leaking a real `team_state.json` into the repo working directory during `pytest` runs, unnoticed until manually caught via `git status`. Fixed with a new repo-wide `tests/conftest.py` autouse fixture isolating both `LEADERBOARD_FILE` and `TEAM_STATE_FILE` per test — individual test files' own local isolation fixtures (the pre-existing `isolated_leaderboard` pattern) still work layered on top, same `tmp_path`, no conflict.

**Team identity has no account system, by design.** Registration is a free-text pseudonym (`st.text_input`), not tied to email or any student identity — matches the FERPA framing. Real consequence: if a team's members open the app on **separate devices** without using Driver/Follow Along roles above, they are *not* synced — each gets an independent session (own decisions, own budget state). A Driver-less multi-device team should still share one device/browser tab.

**The leaderboard is a plain local file** (`leaderboard.json`, `utils/game_state.py::LEADERBOARD_FILE`), not a database. Whether different teams' scores land on one shared leaderboard depends entirely on deployment:
- **One central server** (e.g. Streamlit Community Cloud, one URL the whole class visits) → genuinely shared leaderboard. This is almost certainly the right model for a class and isn't set up yet — worth doing before real use.
- **Each student runs it locally** → every instance has its own separate file; nobody's scores are actually comparable.

**Sidebar is nav-only as of 2026-07-24** (Team Registration, Active Network selector including Movies) — no gameplay decisions live there anymore. This was the end state of a two-step consolidation:
- **2026-07-22** (user feedback: "too many options for a simulation") — went from 5 sections to 3 (Team Registration, Active Network, Budget Allocation) by removing dead Development/Reserve budget sliders (never read by `pages/simulation.py`'s scoring engine), a stale Quick Checklist (referenced pre-redesign tab names), and the Simulation Year slider (a first attempt always plays Year 1 now; `ss.year` stays fixed at 1 internally, only the ability to change it was removed).
- **2026-07-24** — removed the remaining Budget Allocation/Marketing slider too, since it duplicated the Decisions-phase Financing section's marketing control (the Decisions-phase one is now the only copy). Added Movies as a peer button in the Active Network selector, alongside Oxygen/Bravo/Peacock.

**Real environment gotcha, worth remembering**: this machine has two Streamlit installs on PATH at different versions — `C:\Program Files\Python314` (1.59.2) and Anaconda (`C:\Users\apalo\anaconda3`, 1.45.1). `streamlit run app.py` in a real shell resolves to the **Anaconda one**. A fix verified only against the other install (`st.iframe`, a newer API 1.45.1 doesn't have) shipped broken and threw `AttributeError` in actual use. Fixed with a `hasattr(st, "iframe")` runtime check rather than assuming a version. **Any future change should be verified against `anaconda3\python.exe` / `anaconda3\Scripts\streamlit.exe` explicitly**, not just whatever `python`/`streamlit` a fresh shell resolves to.

## Zach Schlessel (NBCUniversal) — feedback received 2026-07-24, pending triage

Received via email, covers two distinct artifacts — **only the second applies to this repo**:
1. **"Thesis Edits Needed"** (remove Paris Olympics reference; focus on Peacock originals vs. library vs. NBC/Bravo investment value; emphasize cross-platform cost amortization) — reads as edits to the written case study (`Independent Study Case A/B - Comcast NBCU` docs), not this codebase. Not actioned here.
2. **Simulation design feedback** (below) — a real, structural rework proposal for CableOS, not yet reconciled against what's built. Preserved close to verbatim so the actual ask isn't lossy-summarized before a decision is made on scope/priority.

**Major structural tensions with the current build, flagged before any of this is implemented:**
- ~~**"Change from quarters to years"**~~ — **implemented 2026-07-24**, see "Annual turn engine" above. User confirmed the specific structure via a clarifying question: each level (Oxygen/Bravo/Peacock) is now 4 real annual turns, not 4 quarters within a frozen year — matching this feedback's "Multi-Year Dynamics...compounds over a 4-year window" and "Deck 1/Deck 2" language, and the *original* Zach brief's "prove yourself over 3-5 years" framing more closely than the quarterly engine it replaced.
- ~~**Performance-based budget compounding**~~ — **implemented 2026-07-24**, see "Real mechanics built" below. Was a flat 3%/yr; now genuinely tied to each year's actual margin.
- ~~**Paid "research" option**~~ — **implemented 2026-07-24**, see below.
- ~~**Title/IP legal-risk check**~~ — **implemented 2026-07-24**, see below.
- **Amortization reassigned from network to content category** — currently Oxygen=36mo/Bravo=12mo/Peacock=36mo (network-based); this feedback proposes True Crime=24mo vs. Non-True-Crime=12mo (genre-based), independent of which network airs it. **Still deliberately not implemented** — would retroactively change the cost burden on Oxygen's mostly-True-Crime slate and could break the calibrated 12%/15%/10% pass thresholds. Needs a scope decision (replace vs. layer on top of network base) before touching it.
- **Budget model reframed as abstract 0-100 allocation sliders** — needs a value-mapping decision (what does "73" mean in dollars?) before it can be built without guessing. Not yet implemented.
- **AI-graded custom show-concept option** — needs an actual LLM API call wired up (secrets/cost implications), a different kind of change than anything else here. Not yet implemented.
- **Movies category rework** (sequels/new-IP/kids/horror-indie replacing Day 2's current risk-adjusted-NPV/genre model) — would touch the tested, calibrated Day 2 engine (46 passing tests). Not yet implemented, needs explicit scope agreement first.

## Real mechanics built 2026-07-24 (beyond the quarters-to-years engine and the 3 charts)

Three more items from Zach's feedback, each genuinely wired into gameplay (not illustrative, not decorative):

1. **Performance-linked budget** (`utils/models.py::performance_linked_growth()`) — replaces the flat 3%/yr `annual_budget()` growth. `ss.level_budget` is now real state: clear the level's pass threshold in a given year → next year's budget grows 8%; land between 0 and threshold → the old 3%; go negative → a 6% cut. Wired into the year-end transition in `pages/simulation.py` (captured in each `yearly_log` entry as `"budget"`, reset alongside other per-level state on network switch/advance/restart). Financing shows the real number with an over-budget warning (content cost + marketing vs. `ss.level_budget`) where none existed before (the old sidebar's budget-cap warnings were removed earlier the same session along with the sidebar's redundant marketing slider — this restores real budget feedback, just performance-linked instead of fixed). Renewal's Budget Bridge shows next year as a range (best/worst case) rather than a false-precision single number, since it genuinely isn't knowable until this year's results are in.
2. **Paid Research** (`pages/renewal.py`, `utils/models.py::preview_show_variance()`/`variance_to_stars()`) — $2M/show, deducted immediately from `ss.level_budget`. Reveals a real 1-5 star signal by replicating the *exact* seeded RNG sequence `_compute_year()` will use for that team+year (same seed formula, same per-show draw order) — a genuine preview of the actual upcoming outcome, not a decorative random number. `ss.research_revealed` tracks per-show-per-year reveals, reset with other per-level state.
3. **Title/IP legal-risk check** (`pages/greenlight.py`) — a "Show Name / Concept" matching any real title already in `utils/data.py`'s slates (Bravo/Oxygen/Peacock combined) triggers a legal-risk banner and blocks the P&L build entirely (`return` before any calculation), matching "Risk = 0 score: take an existing show title without permission, gets sued." Reuses existing show data as the protected-titles set rather than a separate hardcoded list.

All three verified via a single `AppTest` run through a full 4-year Oxygen level (with research purchases each year) → Submit → Advance to Bravo → Movies nav, no exceptions, `level_budget` correctly reset to Bravo's own base ($220M) on advancing.

Next step on the remaining items (genre-based amortization, 0-100 sliders, AI-graded show creation, movies category rework): agree scope/priority with the user before writing more code — each has a real conflict or missing decision, spelled out above.

**Three additive charts built 2026-07-24** (after the annual engine, on the user's request to visualize themes from this feedback "without upsetting anything" — no mechanic/formula changes, existing math surfaced or reused):
1. **Genre decay-curve chart** (`pages/renewal.py`) — plots `Show.projected_rating()`'s existing ip_score-driven maturation forward 6 years, grouped by genre. Answers Zach's own open question "how visible should decay curves be to students?" by making math that already existed visible for the first time.
2. **Linear vs. Streaming economics chart** (Financing, `pages/simulation.py`) — runs the team's own portfolio-average show through the existing `greenlight_linear`/`greenlight_svod` formulas at 3 year-checkpoints, visualizing Zach's "linear has higher CPM, Peacock has lower sub acquisition but originals drive usage" framing with the team's real numbers instead of a generic hypothetical.
3. **Year-over-year performance vs. budget chart** (Complete phase, `pages/simulation.py`) — actual OCF margin per year plotted against the real fixed-3%/yr budget line *and* an illustrative performance-linked budget line (margin ≥ threshold → +8%/yr, 0 to threshold → the real +3%, negative → −6%) computed from the team's actual results. Explicitly illustrative — does not change `annual_budget()` or any real budget mechanic; visualizes what Zach's "good year = more budget, poor year = a cut" idea would have looked like without committing to it yet.

Verified via `AppTest` through a full 4-year level (Decisions → Results ×4 → Complete) — all three charts render with no exceptions at every year. Still not human-browser-verified, same standing gap as everything else in this doc.

---

> **Streaming Entertainment Simulation: Zach's Feedback**
>
> **High Level Strategic Focus**
>
> *Core Business Shift (2018 to 2026)*
> 2018: Arms dealer model, build your own streamer, or buy smaller player (Tubi, Star, Showtime). 2026: Competing with FAANG, big talent deals (Taylor Sheridan), high sports rights costs, can't shut down Peacock, decade-long content rights, must partner with tech or pursue M&A.
>
> *Key Strategic Emphasis for Case*
> Shift from subscription growth to profitability. Prioritize profitability over subscriber acquisition. Studio business model changes: less profit from traditional window, incentives need to align with talent. No monoculture anymore: streaming built on old sitcoms and dramas, broadcast can't compete.
>
> *Thesis Edits Needed*
> Remove Paris Olympics reference (too far out). Edit to focus on value of Peacock originals vs. library vs. investment in NBC/Bravo. Emphasize how streaming amortizes costs across platforms.
>
> **Peacock Business Model Context**
>
> *Three Platform Structure*: Peacock (standalone streamer), NBC (linear network/broadcast), Bravo (linear cable).
>
> *Key Financial Dynamics — Linear (NBC/Bravo) Advantages*: Higher CPMs, more ad spots per hour (40-min shows, 20 min content), better ad revenue per hour, strong brand ecosystem, lower marketing spend required.
>
> *Peacock Challenges & Opportunities*: Lower sub acquisition than linear. Originals drive more subscriptions and usage; greater variability in performance. Library is cheaper but harder to predict — attracts fewer subs but is profitable (programmatic advertising). Sports as loss leader: NFL season is 5 months, massive sub spike, then churn.
>
> *The Profitability Curve Problem*: Amortization curve — studios must spread content costs across window and time. Overinvestment risk — entertainment slate can overinvest to fill content needs. Sports dynamics — NFL brings millions of subs for 5 months, requires originals to retain them for the remaining 9 months. Tier breaker — originals win long-term for brand equity and differentiation.
>
> **Core Student Decision Framework**
>
> *Business Strategy Layer*: Understand macro shifts (subs vs. profitability). Balance sports investment with entertainment profitability gap. Align budget discipline with creative flexibility. Consensus build: platform value, originals value, talent relations, ad sales, press benefits.
>
> *Creative Alignment*: Don't tell creatives exactly what to do — give guidelines and direction (e.g., "only 4 originals, certain areas to play"). Example: "We're moving away from horror/supernatural, moving toward prestige drama." Balance with talent sensibility: biggest hits break the mold, can't be engineered by research/finance.
>
> *Business Side Skills Required*: Financials must make sense AND convince creatives they align with creative vision. Acknowledge intangibles and qualitative considerations. Weigh opportunities and risks. Use case studies from other streamers to back decisions.
>
> **Simulation Structure**
>
> *Time Horizon*: Change from quarters to years (not quarters).
>
> *Annual Workflow — Order of Play (Year 1 Planning - Q1)*: Portfolio Review (spot cash cows vs. dogs in slate) → Renewal Decisions (cancel low-ROI shows to free budget) → Sidebar (adjust marketing and reserve allocation) → P&L Check (confirm income statement is healthy) → Portfolio Submit (lock in official score).
>
> *Carry-Forward from Previous Year*: Expected 5-10% decline on renewed shows (base case). Marketing assumed flat. Students can pay for research to examine decline scenarios.
>
> *Launch Timing Decision*: "Which quarter would you launch these shows and why?" Strategic consideration: how does timing shape brand equity?
>
> *Budget Constraints*: Total spend budget (e.g., $100 million). Students allocate content spend vs. marketing spend. Minimum thresholds (e.g., $3M marketing minimum for show viability).
>
> **TV Shows / Series Simulation**
>
> *Content Categories*: True Crime (lower ratings, better decay rate, lower CPM, amortizes over 2 years) vs. Non True Crime (higher ratings, higher decay rate, higher CPM, amortizes over 12 months). Different revenue profiles require different strategic positioning.
>
> *Simulation Mechanics — Deck 1: Oxygen / Basic Model*: 100 million annual budget. Allocate 0-100 on content, 0-100 on marketing. Content generates ratings, marketing impacts performance. Repeats fill 24/7 schedule for 52 weeks.
>
> *Deck 2: Bravo / Advanced Model*: Repeat erosion and decay modeling. Decay rate randomized by genre. Students pick slate that nets to budget. Shows available in specific years (production delays).
>
> *Revenue Calculation*: `Revenue = (Rating × CPM × Ad Spots per Hour) × Decay Curve × Hours per Day × 365 Days + Research Cost (optional) + Talent/Risk Adjustments`.
>
> *Performance Variables*: Ratings Band (varies by talent attached, genre, title testing). Decay Curve (genre-dependent decline). Marketing Spend Relationship (~1 percentage point rating increase per $1M, threshold dependent). Research Option (pay for a 1-5 star rating prediction — 1 star = 30% decline, 5 star = 10% growth).
>
> *Renewal & Greenlight Decisions*: Renew existing shows (with expected decline). Greenlight new shows (within budget). Make production choices (some shows available only Year 2+). Students balance risk: pay for research or gamble on instinct.
>
> *Risk & Title Compliance*: Risk assessment for each show (legal, market, creative). Risk = 0 score: take an existing show title without permission (gets sued) — originality matters.
>
> *Show Creation Option*: Students can propose a custom show (title + 3-4 sentence description); AI grades the concept and determines if testing/research is worth paying for.
>
> **Movies Simulation**
>
> *Same Year 1 Practice as TV*: Budget allocation, greenlight decisions, research vs. risk-taking.
>
> *Movie Categories*: Sequels, New Adult IP, Kids IP, Horror or Indie — different profiles with different revenue streams.
>
> *Windowing Strategy (Year 3 Introduction)*: Trade-off decisions — theatrical vs. streaming vs. PVOD vs. rental. Window length impacts box office revenue vs. streaming revenue; earlier streaming window hurts theater but helps streaming. Genre determines licensing/merchandise potential.
>
> *Revenue Streams*: Box office (theatrical window), licensing revenue (VOD, streaming, rental), theme park/merchandise opportunities, Universal licensing deals ("pay yourself" model).
>
> *Constraints*: Limited film slots (e.g., "10 films or 3 films"). Windowing rules apply year 3 onward. Genre determines theme park eligibility.
>
> **Grading & Scoring**
>
> *Evaluation Criteria*: P&L Health (does budget balance, is profitability trending right). Portfolio Strategy (are cash cows protecting losses appropriately). Renewal Logic (smart cancellations vs. emotional attachments). Marketing Discipline (spend threshold met, ROI positive). Creative Alignment (do chosen shows reflect stated strategy). Brand Equity (long-term positioning — originals vs. library balance).
>
> *Randomization*: Performance bands for new shows (talent, genre, testing dependent). Starting-point decay curves randomized. Production availability randomized. Shows can fail for legitimate reasons (talent departure, poor script, etc.).
>
> *Multi-Year Dynamics*: Year-over-year budget adjusts based on performance — good performance means more budget allocation, poor performance means a cut for the following year. Compounds over a 4-year simulation window.
>
> **Key Pedagogical Takeaways**
>
> *What Students Learn*: Financials & strategy integration (how numbers drive creative decisions). Platform dynamics (linear vs. streaming profitability profiles differ). Risk management (when to research vs. when to bet on instinct). Talent relations (financials only work if creatives are aligned). Long-term thinking (short-term wins vs. brand equity). Constraints (real production, talent, and budget limitations).
>
> *Case Study Scaffolding*: Start simple (Oxygen — basic spend allocation) → build complexity (Bravo — decay curves, genres, repeats) → add realism (multi-year — budget pressure, cumulative effects) → introduce windowing (movie distribution strategy).
>
> **Open Simulation Decisions**
>
> Will students manually create show titles or select from a catalog? How frequently does production availability randomize? Should budget constraints shift mid-year (emergency cancellations)? How visible should decay curves be to students (hidden vs. transparent)? Can students see competitors' slates (comparative analysis)? How are tie-breaker decisions (same ROI) handled?

## Original mechanics brief (Zach Schlessel, NBCUniversal — preserved verbatim in spirit)

Recovered from an untracked `Zach Notes.docx` found in a stale duplicate clone (`OneDrive/Desktop/ComcastSimulator`) — preserved here since it's the closest thing to a founding design doc and wasn't committed anywhere.

> Let's pretend it's 2012 — the beginning of the linear downturn. People are cutting the cord, streaming, belts are tightening, cable's growth period has ended. Managing portfolio allocations across different networks — in 2012 every network has individual leadership, focused on sole P&Ls. You have a network like Bravo — has built a solid stable of shows (Below Decks, Housewives), needs to focus on diversifying content mix while being beholden to pretty strict budget constraints: $750K/episode, 5% uptick each year to renew, 3% uptick in budget, 12-month amortization curve — every month you pay 1/12 of total freight of the show, you pay the first of the month. On March 30th, I am paying for 29 days of no revenue of the show.
>
> Year 1 you have a list of 20 shows, they all have some estimate against them. There's a research department to forecast what the ratings of the shows are; a scheduling team tries to optimize schedule to maximize short-term ratings and long-term IP development; finance team calculates ratings and costs, makes sure the costs hit the budget numbers, and we are maximizing revenue against cost allocation. Land all of the shows on the schedule, try to hit the budget — if you have extra money, allocate it to marketing or more shows. ROI on marketing vs. ROI on show without marketing. Dummy distribution revenue if we know there are X million subs of network X in 2012, 5% distribution rev, calc ad sales. Play that out 10 years.
>
> In year 2, different budget — they're going to have to choose to renew/cancel based on costs and revenue associated with it. Prove themselves over the first 3–5 years that they are maximizing their OCF; if they do a good job, take on a second network — Oxygen, cheaper content, $300K an episode, 3-year amortization curve — then they have to allocate funds across those two networks. If they do well on that, year 6, 8, or 10, the company chooses to launch a streaming network.
>
> When a show launches you start paying for the show in the month it airs — if I debut a show March 1st, 10 episodes at $750K/12, I pay this number in March, whether March 1st or March 30th. If you launch a show on March 30th you are accumulating some revenue and paying for a show that is not accumulating revenue — how do you fund your cash cows vs. your other shows, how you will make these decisions. Put it all to linear to maximize short-term profits, or others may say do a lower OCF in hopes of a better long-term future — for every dollar spent, higher return on linear vs. SVOD, but this should change over time.
>
> Dummy green light model: a great show — the pitch — NBC original — do you put it on NBC or Peacock? Green light model: this is rating on linear, this is performance on Peacock in terms of number of paid adds, LTV curve from subscription revenue, engagement of the show — you could build this P&L on what it would look like on linear vs. SVOD. Build out a revenue ratio: every hour of every day gets a forecast in terms of how many people watch, but we can add a ratio on top of this so that this is easier for students. Revenue for premiere episode for each of them.
